# ダイスダンジョン 引き継ぎ資料

最終更新 2026-08-29 ／ 対象 `index.html` 5447 行・828 KB

この資料の数字は、書いた時点の `index.html` から機械的に取り出したもの。
自分の目で確かめ直すときは `node tools/probe.js` を叩く。

---

## 1. これは何か

ダイスの目で結果が決まる、テキストベースのローグライクRPG。
1周ごとにキャラを作り直し、装備もスキルも持ち帰らない。残るのは図鑑の知識だけ。

**Slay the Spire 型の構造**を土台に、以下を足している。

- 手動操作の前衛後衛制ターン戦闘
- 職業・種族・欲望の3軸によるビルド（4 × 4 × 7 = 112 通り）
- 属性10種と状態異常10種
- 素材・食材からの鍛冶と料理

### 動かし方

`index.html` をブラウザで開くだけ。ビルド手順もパッケージ管理もない。

外部依存は Google Fonts の `DotGothic16` のみ（読めなければ等幅にフォールバックする）。

### リポジトリの中身

```
index.html                          本体。839 KB・5998 行。これ1つで動く
tools/probe.js                      Node でデータを検証するための足場
tools/bump.js                       版（VERSION）を1つ上げる
README.md

audio/bgm/                          BGM。場面ごとに1曲（区画によっては複数）
  遥かなる伝説.mp3                     ホーム・キャラ作成・遊び方・見立て
  そろそろ帰ろっと！_M325.mp3            殿堂・潜行の結果
  オルゴールファンタジー.mp3             図鑑
  深域で待つもの.mp3                   ボス戦
  PerituneMaterial_Wonder2_loop.mp3   原始の草原
  樹木の息吹.mp3                      木漏れ日の森
  閉ざされし都（inst_ver.）_2.mp3        苔むした洞窟
  五月雨_2.mp3                        水没した回廊
  PerituneMaterial_Whisper2.mp3       水没都市アトランティス
audio/sfx/                          外から読む効果音。合成で出せないものだけ
  jajan.mp3                          討伐したとき
```

**音は2つの置き場に分かれている。**
`BGMDIR="audio/bgm/"` と `SFXDIR="audio/sfx/"` の2つだけを見ているので、
場所を変えるならこの2行を直せばよい。
合成でつくる効果音（`SND`）はファイルではなく `index.html` の中にある。

`file://` で直接開いても鳴るが、ブラウザによっては読み込みが止められるので、
確認するときは `python3 -m http.server` などで配ったほうが確実。

曲が入っていない場面（世界樹の森）は無音になる。
`BGM` に登録が無いと `Music.play()` がそのまま `stop()` に落ちる。

**ファイルが無いときは黙って落ちる。**
BGM は再生の約束が壊れて `now` が戻るだけ。外から読む効果音は
`bad` の印を付けて二度と試さず、合成の音に落ちる（`SFX.win()` 参照）。
だから mp3 を置く前でも遊べる。

### 版（バージョン）

`index.html` の `VERSION` 定数。設定画面の右下に出る。

```
node tools/bump.js          α0.0001 → α0.0002
node tools/bump.js --show   いまの版を見るだけ
```

**マージのたびに1つ上げる。** 桁は保たれ、α0.0999 の次は α0.1000。

### ファイルの内訳

| | バイト数 | 割合 |
|---|---|---|
| 効果音（base64 の MP3 12種） | 555 KB | 69% |
| それ以外のスクリプト | 213 KB | 26% |
| CSS | 33 KB | 4% |

**7割が効果音**。これ以上増やすなら BGM と同じく外部ファイルにすること。

---

## 2. 遊びの流れ

```
タイトル（ホーム）
  ├ 潜る  → キャラ作成（5段階）→ 探索 → 戦闘 → …
  ├ 殿堂  → 過去のキャラの記録
  ├ 図鑑  → 6カテゴリ・全278件
  ├ 見立て → 3問の診断でビルドを提案
  └ 遊び方 → チュートリアル
```

画面は `<section>` 8枚を `showScreen()` で切り替えている。
`tut` `quiz` `home` `hall` `codex` `make` `floor` `fight`。

### キャラ作成（段階式）

職業 → 種族 → 欲望 → 確認 → 潜る場所 → 潜行開始。
種族を選んだ時点で立ち絵が出る。各段階で1つ戻れる。

### 探索

15段 × 3列。道は4本（`PATHS=4`）引かれ、交差しないようになっている。
ボスは 16 段目に1つだけ置かれ、15 段目のすべてから繋がる。

| 部屋 | 出現 | 内容 |
|---|---|---|
| 戦闘 | 重み45 | 段の深さで敵の規模と強さが変わる |
| 事象 | 重み22 | 待ち伏せ30% / 宝箱25% / 泉25% / 行商20% |
| 精鋭 | 重み16 | HP×1.4 STR×1.15 |
| 休息 | 重み12 | HP45% MP60%回復。ここで料理・鍛冶ができる |
| 行商 | 重み5 | 装備3・道具3（傷薬は必ず並ぶ）・レシピは60%で1件。**1つの品につき1つまで** |
| 宝物 | 固定 | 9段目。金貨＋食材＋道具、35%でレシピ、そのあと装備3択 |
| ボス | 固定 | 16段目 |

固定枠が3つある。**1段目は必ず戦闘、9段目は必ず宝物、15段目は必ず休息**（ボスの直前）。

配置の制約（`genAct()`）：

- 5段目より手前に精鋭・休息は出ない
- 14段目に休息は置かない（15段目が休息なので連続する）
- 精鋭・行商・休息は親と同じ種類にならない
- 同じ親から分かれた道の行き先は種類を変える

戦闘の中身は段で変わる。1〜2段は単体、3〜4段で複数が混ざり、
7段以降は `norm`、11段以降は `hard` を2倍の重みで引く。

### 戦闘

DEX順に行動。敵の行動は2ターン先まで予告される。

---

## 3. 数式

コードから起こしたもの。`dieRate` `perHit` `threshold` `powOf` が実体。

### 命中閾値

```
閾値 = 4
     + 盲目の値
     − 図鑑Lv2以上で 1、Lv4以上でさらに 1
     + 遠隔武器で自分が前衛なら 1
     + 遠隔武器で相手が後衛なら 1
     − スキルの thr / 集中の値
     − 「格付け」で相手の最大HPが自分より低ければ 1
     + floor((相手DEX − 自分DEX) / 15)     ← 0 未満は 0
閾値は 2〜6 に丸められる
```

### 威力

```
威力 = 能力値
     × (1 + パッシブ statSTR/statDEX/statINT/statVIT の和 /100)
     × 武器倍率 (WEP[].mul)
     + 道具・料理の加算 (opt.pow)
     × スキル倍率 (opt.powMul)
```

固定値加算は基本的に使っていない。能力値が1周で2〜3倍に伸びるので、
固定値のボーナスは終盤に薄まるため。

### ダメージ

```
1ヒットの威力 = max(ceil(威力 × 25%), 威力 − 相手のDEF)
出目の上振れ  = min(25%, max(0, (出目 − 閾値) × 5%))
              ※「激昂」持ちが 6 を出したときだけ上限 35%
1ダイスの被害 = max(1, round(1ヒットの威力 × (1 + 上振れ)))
```

ダイスは武器の `hands` 本（＋スキル・パッシブ・索敵のボーナス）振る。
閾値以上が出たダイスだけ当たり、それぞれ独立にダメージを出す。

### 属性

```
魔法  火 → 土 → 雷 → 水 → 火   （光 ⇄ 闇 は相互に有利）
物理  斬・叩・貫
```

敵は弱点1つ・耐性1つ・自属性1つを持つ。

```
弱点を突いた      ×1.50
属性相性で有利    ×1.35
属性相性で不利    ×0.75
耐性に当たった    ×0.70
```

掛け合わせたうえで **0.60 〜 1.70 に丸める**。
弱点かつ相性有利なら 1.5 × 1.35 = 2.03 → 1.70 で頭打ち。

**弱点を突いた瞬間に図鑑へ記録される。** 初見では見えない。

### 敵の攻撃の強弱（`pct`）

**敵の `atk` は割合で強弱を持つ。**

```javascript
{n:"抱き潰す",k:"atk",dice:2,pct:40,w:3}    // STR の 140%
{n:"粘液を撒く",k:"atk",dice:4,pct:-35,w:3}  // STR の 65%（手数で稼ぐ技）
```

`powOf` が `Math.round(u.STR*(1+pct/100))` を作り、そこに強化と弱体を足す。

**固定値（`pow`）だと深い区画で意味が薄れていた。**
`makeFoes` が STR を深さで伸ばすのに `pow` は伸びないので、
アトランティスでは「+16 の重い一撃」が STR 78 に対して 2 割ぶんしかなく、
軽い攻撃との差が潰れていた。割合なら どの深さでも同じ手触りになる。
（切り替えで 原始の草原はほぼ変わらず、深いところで 3〜8% 重くなった）

**強化（`buff` / `cheer`）は固定値のまま。**
あちらは「その行動そのものの重み」ではなく後続の攻撃に乗る値なので、
割合にすると二重に掛かる。

`telePreview` と `enemyAct` の両方が `powOf(f,{pct:t.pct||0,pow:t.pow||0})` を
呼ぶ。**予告と実際のダメージは必ず同じ式を通すこと。**
（前は予告側が式を写していて、直すたびに片方だけずれる作りだった）

### 構え（`me.block`）は殴られるまで残る

`me.block` は **1ヒットごとの DEF 上乗せ**。減るのではなく、その値のぶん
毎回のヒットを軽くする。

**落ちるのは「実際に殴られたとき」だけ。**
ダメージが通った時点で `me.blockSpent=true` を立て、次の自分の手番で
そこを見て 0 に戻す。誰も殴ってこなかった回はそのまま持ち越す。

```javascript
if(startHP>endHP){ me.hurtThisRound=true; if(me.block>0)me.blockSpent=true; }
…
if(me.blockSpent){me.block=0;me.blockSpent=false;}
```

**`me.hurtThisRound` は使えない。**あれは `endRound()` が毎ラウンド false に
戻すので、自分の手番から見ると常に false になっている。

前は自分の手番のたびに無条件で 0 にしていたので、
**相手が守りや溜めに回った回は、防御に使った1手が丸ごと消えていた。**

積み重ねの心配は要らない。殴られれば全部落ちるので、
安全な回に積んでも手番を捨てるだけになる。

### ボスの段階（`phases`）と 隙（`open`）

**段階** … HP がしきい値を切ったら行動表ごと入れ替える。1段につき1回。

```javascript
phases:[{at:.5, n:"核 が 浮 か ぶ", d:"殻が薄れ、中の核が透けて見えている",
         def:-9, str:8, heal:.08,
         acts:[ … ]}]
```

| 鍵 | 意味 |
|---|---|
| `at` | 最大HPに対する割合。これを切ると入れ替わる |
| `acts` | 差し替える行動表（元の表は捨てる） |
| `def` `str` `dex` | DEF の増減／STR の増減（%）／DEX の増減 |
| `heal` | 最大HPに対する回復（そのぶん戻して仕切り直す） |
| `quiet` | 盤いっぱいの名乗りを出さず、ログと小さな表示だけで済ませる |
| `noParts` | HP ではなく「部位が全部落ちたら」で入れ替わる |

`checkPhase(f)` が **こちらが殴ったあと・道具のあと・継続ダメージのあと** の
3箇所から呼ばれる。入れ替えたら `f.tele` と `f.next` を引き直すこと。
**古い予告のまま殴られると、画面に出ていた技と違うものが飛んでくる。**
`prepareBattle` が `f.ph=0` と `f.acts=FOE[f.key].acts` に戻すので、
2回目に戦っても素の表から始まる。

**段階はボス専用の仕組みではない。**ふつうの敵にも付く
（ストーンワームの 殻 を 締 め る ＝ HP半分で DEF +10）。
そのときは **`quiet:true` を付けること。**
道中の敵で毎回1.25秒の全画面を挟むと、探索がひどく重くなる。

**隙** … 大振りの技を振ったあと、自分に防壊と鈍足をかける。

```javascript
{n:"核 を 振 る",k:"atk",dice:3,pct:20,open:{t:2},w:3}
// open.t   … 効いているターン数（既定 1）
// open.def … 防壊の量（既定は その敵の DEF の6割）
// open.slow… 鈍足の量（既定 14）
```

`enemyAct` の最後で `opening()` が走る。`telePreview` が
「振ったあと 隙ができる」と出すので、**待って叩く読み合いになる。**

#### そのほかの道具立て

| 書き方 | 何が起きるか |
|---|---|
| `{k:"hex", fear:1, dur:2}` | **恐 怖**。命中閾値が上がる（`blur` と足し算） |
| `{k:"buff"/"cheer", pctUp:30, dur:3}` | 威力を**割合で**上げる。掛けた時点の STR から量を出して固定値の器に入れる |
| `{k:"atk", ail:{curse:8,t:2}}` | **当たったときだけ**状態異常を乗せる。`curse`(DEF−) `slow` `weak`(威力−%) `blind` `poison` |
| `{k:"summon", num:3}` | 呼ぶ数が 1d`num`（既定 1d3）。前列が満杯なら**後列へ回る**。`back:false` で後列を禁じる |
| `phases:[{forceAct:"分裂する"}]` | その段階に入った瞬間、**次の手をこれに決める**。「半分を切ったら一度だけ○○」に使う |
| `{k:"hex", weakPct:10, dur:2}` | 威力を**割合で**下げる。`weak:1` は 15% きざみなので、端数の要る場所はこちら |
| `{k:"summon", ifUnder:2, count:["cspider","qegg"]}` | `count` の相手が場に `ifUnder` 体以上いたら**呼ばない**。無限に増えるのを止める弁。`count` を省くと `who` を数える |
| `{k:"idle", d:"…"}` | 手番は使うが**何もしない**。`d` がログと予告に出る |
| `{k:"hatch", who:"cspider"}` | **自分が消えて、中身が同じ立ち位置に立つ。**卵に使う |
| `{k:"heal", pct:.1}` | **呼んだ相手**（`f.owner`）の HP を最大の `pct` だけ戻す。既定で自分は散る（`wither:false` で残る） |
| `{k:"mend", full:true, cd:3, max:2, at:.6}` | **味方ひとり**（いちばん深く傷ついた者）を癒やす。`full` で満タンまで。`cd` は再使用待ち、`once`/`max` は回数、`at` は「残り○割を切った味方がいるときだけ出す」 |
| `{k:"summon", back:false}` | **後列に回さない。**前列が埋まっていたらその手を選ばない。摘めない場所に置かないための札 |

**`forceAct` で指す手は、その段階の `acts` にも置くこと。**
段階に入ると `f.acts` が丸ごと入れ替わるので、素の表にしか無い手は引けない。
`hidden:true` `w:0` にしておけば、勝手には選ばれない。

#### 効いているものを出すところは1つ（`stateRows`）

盤の小さな札（`ailText`）も、押したときの一覧（`ailList`）も、
**`stateRows(u)` から作る。**足すときはここへ1行足すだけでよい。

前は `ailText` の中に直接書いていたので、
**効いているのに画面のどこにも出ない状態が9つあった**
（狙い・纏い・獣の力・気迫・金の力・持続の構え・反撃・疾走・誓い）。
**新しい状態を `me.○○` に足したら、必ず `stateRows` にも足すこと。**

残りターンの数え方は、告知に出している数と揃える。
状態異常は `t+1`（`hex` の予告が「`dur+1` ターン」と言うため）、
強化は `t`（`buff`/`cheer` の予告が「`dur` ターン」と言うため）。

#### 連撃は 追加ぶんから さらに増えない

```javascript
const q=rolls.filter(v=>v>=6).length;
const add=[];for(let i=0;i<q;i++)add.push(r6());
```

前は追加ダイスの 6 も数え直していたので、6 が続くかぎり際限なく増えた。

**上振れの一覧（Lv17・ヒューム・強欲・対サハギン 実効HP495）**

| | ダイス | 平均 | 上位1% | 最大 |
|---|---|---|---|---|
| スカウト 素 | 5d6 | 125 | 304 | 385 |
| ＋連撃（前） | 5d6→ | 150 | 454 | **766** |
| ＋連撃（後） | 5d6→ | 146 | **385** | 681 |
| ＋連撃＋二段突き | 7d6→ | 205 | 527 | 762 |
| 双短剣＋連撃＋二段＋急所 | 8d6→ | 217 | 535 | **876** |
| ナイト 素 | 3d6 | 159 | 328 | 333 |

**上振れの多くは 連鎖ではなく「もともと振るダイスの数」から来ている。**
連鎖を止めても いちばん大きい上振れは 766→681 としか動かない。
**さらに抑えるなら 手数そのものか、1回の攻撃の上限を触ることになる**
（案：追加ダイスの威力を落とす／与ダメージに「平均の◯倍」の天井を置く）。

#### デバフが乗るかは 専用に1回振る（`ailRoll`）

```javascript
const AILTHR=1;
function ailThr(u,tgt,opt){ return Math.max(2,threshold(u,tgt,opt||{})-AILTHR); }
```

**武器のダイス数とは切り離す。**前は「武器のダイスが1つでも当たれば付く」だったので、
**手数の多い武器ほどデバフが得意**という、腕とも術とも関係ない差が出ていた。

| | 前 | 後 |
|---|---|---|
| ナイト（3d6・デバフ技2） | 88% | 83% |
| スカウト（5d6・デバフ技1） | 87% | 67% |
| **ウィザード（3d6・デバフ技6）** | **42%** | **67%** |
| 敵 → こちら（`hex`） | **100%** | 50〜67% |

`hex` は乗らなかった回もログに出目を残す。予告には「通る率 ◯%」を出す。
**攻撃に付随するデバフ（`atk` の `ail`）は、当たったうえで さらにこの判定を通る。**

**避ける腕が そのままデバフの抵抗になる**ので、CON のような能力値は要らない。

#### 〔削除〕遠隔武器で前に立つ罰

`u.wep.type==="ranged" && u.rank==="front"` で命中閾値 +1 していたのを外した。
**前に立つ理由が他に無いのに罰だけがあり**、杖のウィザードは
立っているだけで閾値が上限（6）に張り付いていた。

外したことで **アーチャーとウィザードの与ダメ/T が 3〜5割 上がる**
（Lv13 対ゴーレム：アーチャー 113→151、ウィザード 107→160）。
**ウィザード＋エルフが 175 で全職の最上位になった**ので、
遠隔が強すぎると感じたら ここを見ること。

#### 状態異常に対する手

**解く手と、放置する重さを 対で置く。**片方だけでは遊びにならない。

| どこ | 何 | 中身 |
|---|---|---|
| 共通スキル | 気 を 静 め る | `kind:"cure"` `cure1:true`。**1つだけ**解く（残りの長いものから） |
| 道具 | 薬 草 の 束 | `cure1:true`（一等14金）。レシピ `r_herbs`（香草×2＋粘液×1） |
| 道具 | アンチポーション | `cure:true`（二等32金）。**すべて**解く |
| 敵 | `{k:"cleanse"}` | 味方ひとりの状態異常をすべて解く。**背負っている味方がいるときだけ出る** |
| 敵 | `{k:"atk", vsAil:40}` | 相手が状態異常を背負っていたら 威力 +40% |

**CON のような「デバフ耐性」の能力値は入れないと決めた**（2026-09-02）。
7つ目の能力値は 種族4・職4・成長4・保存の形 まで波及するのに、
**持っているだけで盤の上では何も決められない。**
デバフを持つ敵は 18/51体・行動の13% しかないので、
**3戦に2戦は何もしない能力値になる。**解く手を増やすほうが効く。

#### 防御は シールド（`shieldOf` / `afterShield`）

**`u.block` は DEF に混ぜない。**別の器として持ち、
`perHit` で1ヒットぶんのダメージを出したあと、**1ヒットごとに引く。**

```
ダメージ = max(0, hitDmg(perHit(威力, DEF), 出目) − シールド)
```

- **`perHit` の「威力の25%までしか削れない」下限は DEF にだけ効く。**
  シールドには効かないので、**シールドは一撃を 0 まで消せる。**ここが前との一番の違い
- ためた総量から引く器ではないので、**連撃に弱い・単発に弱い という偏りが無い**
- 見切り（`me.half`）は **ダメージを半分にしてから** シールドが引く
- 反撃（`me.counter`）は **シールドで消したぶんも含めた量**（`raw`）で返す。
  受け止めた一撃こそ返せるように

**実測（同じ出目・3ダイスとも5）**

| | 減り |
|---|---|
| 素のまま | 147 |
| シールド18 | 93 |
| 見切り | 75 |
| 見切り＋シールド18 | 21 |

**量は 固定値（`block`）と 割合（`blockPct`）の両方で書ける。**
`shieldAmount(u,t)` が `t.block + u.maxHP*t.blockPct/100` を返す。
敵にもこちらにも同じ関数を使う。

| | 書き方 | Lv13 での値 |
|---|---|---|
| 防御する | 最大HPの **9%**（＋装備の `blkPct`） | 36 |
| 盾 構（ナイト） | **12%** | 48 |
| 鉄 壁（ナイト） | **20%** | 80 |
| カウンター（ナイト） | **10%** ＋ 反撃40% | 40 |
| 鉄 壁（共通・2ターン） | **8%** ／ターン | 32 |
| 殻の盾（装備） | 割合を **+3%** 押し上げる | 9%→12% |
| 敵の guard / veil | 固定 10〜30（据え置き） | — |

**`maxHP` は計算のうえで すでに「素のHP ＋ 装備のHP ＋ パッシブのHP」。**
だから `maxHP × 倍率` と書くだけで「元のHP＋増えたぶん ×倍率」になっている。
**装備で守りを厚くするときは、固定値を足さずに 割合そのものを押し上げる**
（`blkPct`）。単位が揃っていないものは足せない。
HP を上げる装備でも自然にシールドが厚くなる（石甲 HP+85 → シールド 36→44）。

**敵を割合で書くときは 桁が違うことに気をつける。**敵の最大HPは
ネズミ100 からボス2000 まで幅があるので、ボスに 10% と書くと
1ヒットあたり 215 相殺になり、こちらの一撃（約120）が丸ごと消える。
敵に割合を使うなら 1〜3% の桁。

「防御する」1手番が消すダメージは 最大HPの 3.3〜5.9% → **6.5〜11.8%**。

**〔修正〕`hold`（何ターン張り直すか）がどこからも読まれていなかった。**
`me.holdDef` は `runQueue` が読むのに、書く場所が無く、
鉄壁（共通）の「2ターンのあいだ」がずっと効いていなかった。

#### 長引くと 双方 当たりやすくなる（`dragEase`）

```javascript
const DRAGEVERY=5, DRAGCAP=2;
function dragEase(){ return Math.min(DRAGCAP,Math.floor((Math.max(1,round||1)-1)/DRAGEVERY)); }
```

`threshold()` の頭で **双方から** 引く。1〜5R は 0、6〜10R は −1、11R 以降は −2。
硬い者どうしが延々と当たらない膠着をほどくためのもの。

| | 1〜5R | 6〜10R | 11R〜 |
|---|---|---|---|
| ナイト 対 ガーゴイル | 50% | 67% | 83% |
| スカウト 対 ガーゴイル | 33% | 50% | 67% |

**ボス戦は10〜14ラウンドかかるので、後半は必ずこれが効いている。**
被/T の表（`audit.mjs`）は当たる率を 0.55 で固定して測っているので、
**この効き目は表に出ない。**長い戦いの実際の被害は、表より 1.3〜1.7 倍 重くなる。

段が下りた回は `endRound` がログに出す。黙って当たりやすくなると気づけないため。

#### 敵の DEX も深さで伸ばす

`makeFoes` は HP・STR と**同じ倍率**で `DEX` も伸ばす（`1+(dm-1)*0.6`）。
呼ばれた者（`summon`）と孵った者（`hatch`）も同じに揃えてある。

**前は DEX だけ伸ばしていなかった。**こちらの DEX は
スカウトで 53 → 92 と伸びるのに、敵は最大44・中央22のまま。
その結果 `floor((こちらDEX − 敵DEX)/15)` が青天井に育ち、
**スカウトは木漏れ日の森（Lv8）の時点で敵の閾値が全部 6 に張り付いていた。**
そこから DEX が 65 → 92 と伸びても、もう何も変わらなかった。

素早さの差が閾値を動かす幅も `dexStep()` に切り出し、**25 きざみ・上限 +2** にした。

| | ナイト | ウィザード | アーチャー | スカウト |
|---|---|---|---|---|
| 敵の1ダイスが当たる率（前→後） | 43〜50% → 50% | 42〜50% → 50% | 17〜33% → 26〜44% | 17〜29% → 23〜44% |

**副作用：遅い職は深いところで先に動きにくくなる。**
水没都市でナイトが素早さ勝ちする部屋は 13% → 0%。
（伸ばす倍率を 0.35 にゆるめても 4% にしかならず、
そのかわり閾値の直りが半減したので、STR と同じ 0.6 のままにした）

**閾値の式を触ったら `thrWhy` も揃えて、6588通りの照合を回し直すこと。**

#### 命中閾値の内訳（`thrWhy`）

`thrWhy(tgt,opt)` が `{parts,raw,thr}` を返し、`thrPanel()` が
装備・状態の「ステータス」に大きく出す。

**`threshold()` と同じ順で同じものを足すこと。**片方だけ直すと嘘の内訳が出る。
守るために、4職 × 4区画 × 全部屋 × 状態3通り × 技の補正3通り ＝
**6588 通りで `threshold()` と `thrWhy().thr` を照合する**確認をした（ちがい0件）。
閾値の式を触ったら、必ずこれを回し直すこと。

#### 装備で相手の DEF を貫く（`pen`）

`GEAR` の防具・装飾に `pen:25` と書くと、**その割合だけ相手の DEF を無視する**。
`recalcMe` が合計して `m.gearPen` に置き、`playerAttack` が
`passOn("pen")`（パッシブぶん）と足して使う。

**硬い相手ほど効く。**DEF 8 の相手には 2 しか変わらないが、
DEF 31 のゲートガーディアンには 1ヒットあたり 8 変わる。
だから 穿ちの指輪（STR +9・pen 25）と 爪の指輪（STR +13）は、
**相手の硬さで持ち替える関係**になっている。

#### 癒やす敵を置くとき

**後列に隠れて何度も治す相手は、近接しか持たない職を詰ませる。**
`canTarget` は、敵の前列が生きているあいだ近接の狙いを前列に縛る。
だから後列の癒やし手は、前列が落ちるまで殴れない。

サハギンの司祭で計算した例（Lv17・ナイト）：
サハギン 実効HP 495 に対し こちらは 1手番 約130。
**3手番ごとに満タンへ戻されると 390 < 495 で、永久に削り切れない。**

だから **回数の上限を必ず付ける**（`once:true` か `max:2`）。
`cd` だけでは足りない。cd3（4手番に1度）でも、
**こちらが4手番で入れられる 520 が、癒される 495 とほぼ釣り合ってしまう**ので、
上限が無いと延々と続く。
`at:.6` も付けて、**元気な相手に撃って手番を捨てない**ようにする。
（`at` が無いと、かすり傷の味方に全回復を撃って一発を無駄にする）

**部屋の重さを測るとき、癒やす手は 被/T に出ない。**
`audit.mjs` は攻撃しか数えないので、司祭のいる部屋は
表の HP より実際は長い（最大で味方1体ぶんの HP が上乗せされる）。

#### 一撃で落ちる線を見ておく

**被/T が同じでも、1ヒットの大きさで危なさが変わる。**
`○ヒットで最大HPを越える` の境目を跨ぐと、事故死の率が跳ね上がる。

ディープワンの段階で踏んだ実例（こちら Lv17 最大HP463）：

| トライデント | 1ヒット | 満タンから一撃で落ちる |
|---|---|---|
| STR+30% | 230 | **45.8%** |
| STR+25% | 222 | 37.7% |
| STR+22% | 216 | 25.1% |
| STR+20% | 212 | 16.7% |
| **STR+18%（採用）** | **208** | **12.6%** |
| STR+15% | 202 | 12.6% |

**被/T は 248 → 223 と1割しか動かないのに、事故死は 46% → 13% に落ちる。**
`n`ヒットで最大HPを越えるかどうかが崖になっていて、
**6の目が2つ（1ヒット × 2.2）で最大HPを越えるか**がここでの分かれ目だった
（463 ÷ 2.2 ＝ 210）。208 なら 6の目2つで 458 に届かず、3ヒット要る。

**重い一撃を作るときは、被/T ではなくこの線で見ること。**
`1ヒット × 2.2 < こちらの最大HP` を保てば、3d6 の事故死は跳ねない。

#### 部位（`partOf`）と かばう（`guardKey`）

**本体と、本体を守る「部位」でひとつのボスを組む。**
ゲートガーディアンと ガーディアンハンド 右／左 がこれ。

```javascript
gate: { …, guardKey:"ghand", phases:[{ noParts:true, n:"拳 が 砕 け る", acts:[…] }] }
ghand:{ …, noAct:true, partOf:"gate", nm:["右","左"] }
```

| 鍵 | 何が起きるか |
|---|---|
| `guardKey` | この鍵を持つ部位が生きているあいだ、**本体を狙うと部位が身代わりになる**（HP のいちばん多いもの） |
| `partOf` | 本体の**鍵**を文字で書く。`makeFoes` が本体の `id` に直す |
| `noAct` | **手番を持たない。**盤には立ち、狙えるが、`orderOf` が順番から外す |
| `nm` | 同じ相手が並ぶときの呼び分け（既定は A・B・C） |
| `phases:[{noParts:true}]` | HP ではなく「**部位が全部落ちたら**」で段階が変わる |

振り替えは `coverList(ts)` が担当する。**ダイスを振る前に狙い先ごと差し替える**ので、
命中閾値も身代わりのもので出る。範囲攻撃で本体と部位をまとめて巻き込んだときは、
振り替え先が重なるので**1つにまとめる**（同じ拳を二重に殴らない）。

`checkPhase` が3つ面倒を見る。
**部位を叩いたときは本体を見に行き、本体が落ちたら部位も一緒に崩す**
（拳だけ残って戦いが終わらない、を防ぐ）。

#### `seq` は chooseTele の呼ばれた回数で数える

`chooseTele` が返した **N 番目の結果が、そのまま N 手番目の行動になる**
（`prepareBattle` が `tele` と `next` を先に1つずつ引くため）。
だから `seq:3` は「3手番目」でよい。

**以前は seq が当たったときだけ `f.turn` を進めていた。**
`seq:1` `seq:2` を持たず `seq:3` だけを持つ手は、turn が 1 から進まず
**永久に出番が来なかった**（ゲートガーディアンの 連 撃 で踏んだ）。
いまは呼ばれるたびに進める。段階に入ると `f.turn=0` に戻る。

#### 強化の持続は `dur` ターン ちょうど

`addBuff(u,v,t)` は `{v, t}` を積み、`tickBuffs` が **その敵の手番のはじめに** `t--` して
`t>=0` だけ残す。掛けた手番そのものは既に行動を終えているので、
**`dur:2` なら 次の2手番に効く。**

予告とログは長らく `dur+1` と書いていて、**実際より1ターン長く見えていた**（直した）。
状態異常（`hex`）のほうは、こちらの手番の途中で乗るぶん
その回から効くので `dur+1` のままでよい。

#### 手番の連なりを測るとき

**`chooseTele` を何度も呼ぶだけでは足りない。**`forceNext`（溜めて次に必ず出す手）を
持つ敵は、その手が `hidden:true` `w:0` なので候補に現れず、
**いちばん重い一撃が数から丸ごと抜ける。**

サイクロプスで実際に外した。単純に数えると 被/T 49、
手番を連ねて回すと **68**（岩を投げるが 17% を占めていた）。

    for(…){ const t=chooseTele(f); …数える…
      if(t.forceNext)f.forceNext=t.forceNext;      // ← これを回すこと
      if(t.k==="buff"&&t.pow)addBuff(f,t.pow,t.dur||1);
      tickBuffs(f); }

#### 段階の重さのつけ方

**段階は 1.2〜1.4倍 まで。**守りの手（`guard`）を消すと平均が跳ね上がるので、
消すなら攻めの `pct` を下げて釣り合わせる。
（最初に書いたときは守りを全部外して 2.15倍 になり、ディープワンの最終段階は
2ダイスで 806 ＝ こちらの最大HP 463 を一撃で越えていた）

| ボス | 素 → 段階（被/T） | DEF | 段階でやること |
|---|---|---|---|
| キングスライム | 37 → 37 | 18 → 18 | 一度だけ分裂（1d3体） |
| サイクロプス | 67 → 68（咆哮の3ターン **89**） | 16 → 16 | 大咆哮（STR +30%・3ターン） |
| クイーンスパイダー | 49 → 49 | 16 → 16 | 腹を割く（子を1d3体） |
| エルダートレント | 115 → 97 | 30 → 30 | ヒールフラワーを咲かせる |
| ゲートガーディアン | 117 → 119 | 31 → 31 | 拳が砕けて手が入れ替わる |
| ディープワン | 184 → 223 | 30 → 40 | 鱗が染まる（STR+18% DEX+10） |

**数字で殴る段階と、盤を増やす段階がある。**
ゲートガーディアンとディープワンは行動表そのものが重くなるので
`def` を落として釣り合わせる。キングスライム・サイクロプス・クイーンスパイダー・エルダートレントは
**被/T をほぼ据え置き（あるいは下げて）、増える頭数・強化バフ・回復で重くしている**ので
`def` は動かしていない。
**エルダートレントは段階に入ると 115 → 97 と軽くなる。**
そのぶん花に 6〜7手番に1度 最大HPの10% を戻されるので、
**受ける総量ではなく、決着までの長さで重くなっている。**
数字で重くする段階を書くなら **1.2〜1.4倍 まで。**守りの手（`guard`）を消すと
平均が跳ね上がるので、消すなら攻めの `pct` を下げること。

#### 時間で変わる相手（卵）

**卵は「攻撃してこないが、放っておくと敵になる」枠。**
`seq` で手番を数え、最後に `hatch` で中身に化ける。

```javascript
qegg:{n:"蜘 蛛 の 卵", HP:40, STR:1, DEF:2, DEX:6, rank:"front", scout:1,
  acts:[{n:"う ご め く",   k:"idle",  d:"殻の中で、何かが向きを変えた", seq:1, w:0},
        {n:"殻 が き し む", k:"idle",  d:"表面に細い筋が走った",       seq:2, w:0},
        {n:"孵 る",        k:"hatch", who:"cspider",                seq:3, w:0}]}
```

**`seq` は 3 まで要る。2 では間に合わない。**
呼ばれた側は `nf.tele=chooseTele(nf); nf.next=chooseTele(nf);` で
**その場で `chooseTele` を2回**通る。`seq:1` と `seq:2` はここで消費され、
盤に出た次の回にはもう `seq:2` を出している。
`seq:3` まで置いて、**こちらに2手ぶんの猶予**ができる。

盤の枠（前列3・後列3）を1つ食うので、
**卵を置くこと自体が「その枠に子を出せない」ことになる。**
潰すか、母を殴り続けるかの選択になる。

戦利品は `ENCS[sel.enc].list` から出るので、
**組に入っていない相手（卵・花）を倒しても素材は増えない。**

**ヒールフラワー**も同じ作り。`seq:1` が蕾、`seq:2` で `heal` して散る。
呼ばれた側は `owner` に呼び主の `id` を持つので、
**誰に付いた花なのかを見失わない**（呼び主が先に倒れたら「力を送る相手がいない」で空振りする）。

| | 卵（`qegg`） | 花（`hflower`） |
|---|---|---|
| 呼ぶ側 | クイーンスパイダー | エルダートレント（段階後のみ） |
| 手番 | `seq:3` で孵る＝**こちらに2手** | `seq:2` で咲く＝**こちらに1手** |
| 放っておくと | ケイヴスパイダーが1体増える | 古木の HP が最大の10%戻る |
| 立ち位置 | 前が埋まれば後列にも出る | `back:false` で**前列だけ**・`ifUnder:2` で同時に2つまで |

**花が2つなのは、範囲攻撃を持つ職に得をさせるため。**
単体攻撃は1手番にひとつしか摘めないので、必ず片方が開く。
前列をまとめて薙ぐ手（薙ぎ払い／アローレイン／インフェルノ／アースクエイク）なら
**2つとも摘みながら古木にも当たる。**Lv13 で測った値：

| | 単体で古木に | 単体で花に | 範囲で古木に | 範囲で花ひとつに |
|---|---|---|---|---|
| ナイト（薙ぎ払い 70%） | 116 | 158 | 66 | 109 |
| アーチャー（アローレイン 75%） | 112 | 155 | 73 | 116 |
| ウィザード（インフェルノ 75%） | 160 | 203 | 107 | 150 |
| スカウト（範囲なし） | 76 | 119 | — | — |

花の HP は 98（実効）。**範囲は 1発で2つとも落とせる。**
スカウトだけ範囲を持たないので、ここは 追い討ち（倒したらもう一度動ける・1戦1回）で凌ぐ形になる。

### 生まれつきの幸運

`rollLuk()` は **2d6 × 2 ＋ `LUKBASE`(10)** ＝ 14〜34。
`drawMakeConfirm` が初回だけ振り、`lukRollOv()` が宝箱と同じ器
（`#chestOv` と `.d100`）でダイスを見せる。
「選び直す」で戻っても振り直さない。振り直せるのは `newChar()` を通るときだけ。

**下地が無いと 4 まで下がり、その周は宝箱がほぼ空振りだけになっていた。**

### 深さと精鋭

```
深さ倍率 = 1〜2段 0.9 ／ 3〜5段 1.0 ／ 6〜10段 1.14 ／ 11段以降 1.30
          （ボス戦は 1.0 固定）
敵HP  = 基礎HP × 深さ倍率
敵STR = 基礎STR × (1 + (深さ倍率 − 1) × 0.6)     ← HPより緩やかに伸びる
精鋭  = そのうえで HP ×1.4 ・ STR ×1.15
```

### 区画のつながりと世界地図

「ダンジョンの中に階層が並ぶ」という入れ子（`DUNGEONS`／`ACTS`）はやめた。
いまは `AREAS` が区画をフラットに並べ、`to` でつながりを書く一枚のグラフ。

```
原始の草原 ─┬→ 木漏れ日の森 ─┬→ 世界樹の森 → 夜空の洞窟   （洞窟は開発中）
            │             └→ 魔法の森                 （開発中）
            └→ 苔むした洞窟 → 水没した回廊 → 水没都市アトランティス
```

- **深さの上限は無い。** `to` を辿れる限り潜り続け、行き先が尽きたところが踏破。
- 敵の重さは **その区画の `tier`** で決まる。何番目に通ったかでは決まらない
  （`depthAt(tier) = 1 + (tier-1)*0.34`）。
- ボスを抜けたとき `findEntrances()` が `to` のうち
  **まだこの周で通っていない・`wip` でない** ものを候補にする。
  2本あるときは 40% で両方、60% で片方だけ見つかる。
- `wip:true` の区画は地図に「開発中」として出るが、`findEntrances` も
  `routeFrom`（作成画面の道筋）も飛ばすので降りられない。

#### 名前がわかるのはいつか

`areaKnown(k)` が真のときだけ世界地図に名前が出る。偽なら `？ ？ ？`。

| | 名前がわかるか |
|---|---|
| `start` を持つ区画（原始の草原・苔むした洞窟） | はじめから。作成画面で名前を見ているので |
| それ以外 | **その区画を最後まで抜けたとき**（`nextStage` のボス処理で `META.areas[k]=true`） |

入っただけでは残らない。途中で死んだら名前は伏せたまま。
潜っている最中の地図（`mode==="run"`）だけは `RUN.seen` も見るので、
いま立っている区画の名前は出る。

`META.areas` はクラウド保存にも載っている（`firestore.rules` の鍵の並びに注意）。

#### ノード地図の地（`mapBg(H)` / `MAPBG`）

区画ごとの情景を SVG で敷く。節（部屋）と道はこの上に描かれる。
`MAPBG` は「区画のキー → `H` を受け取って SVG 文字列を返す関数」の表で、
キーが無ければ `land`（grass/forest/cave/water）で引き、それも無ければ草原。

| 区画 | 地 |
|---|---|
| 原始の草原 | 遠くの丘、草の株、ときどき立つ穂 |
| 木漏れ日の森 | 幹＋枝＋こんもりした梢の木を、奥は暗く細く・手前は太く。下草と木漏れ日 |
| 苔むした洞窟 | 割れた岩の面を敷き詰め、真ん中をうねる通り道が抜ける。苔の房・ひび・滴る水・たまり水 |
| 水没した回廊 | 奥へすぼまる石畳、両脇の石積みと柱、崩れかけたアーチ、倒れた柱、水面の光と泡 |
| 水没都市アトランティス | 段になって折れたコンクリの廃ビル、抜けた窓と灯った窓、沈んだ光と泡 |

**乱数を使ってはいけない。** `drawMap()` は部屋を選ぶたびに呼ばれるので、
`Math.random()` で置くと押すたびに草木が動く。番号から決まる `bgR(i)`
（`sin` を刻んだ決まった値）で位置を出している。

情景の上には `mapBg` が最後に薄い暗幕（`#11140e` 34%）をかける。
これが無いと節と道が地に埋もれる。**新しい地を足すときは色を派手にしすぎない。**

##### 部屋の地図の作り方（`genAct`）

`PATHS` 本の道を上へ引き、重なったところが分かれ道になる。それだけだと
**2つ以上から選べる節は歩く道の上で 48% しかなく**、一本道が続いて
「選ぶ」場面が薄かった。道を引いたあとに **2本目を足す工程**を入れてある。

```
① 子のいない節（足す途中で作った節）には必ず1本引く … 行き止まりを作らない
② 子が1つの節に、BRANCHTRY の見込みで2本目を足す
```

**線は交差させない。** `(r,nc)→(r+1,c)` があるなら `(r,c)→(r+1,nc)` は引けない。
この決まりがあるので、1段で引ける道は「上の節の数＋下の節の数−1」本が上限。
`COLS=3` では3対3で5本＝**67% が天井**だったので、`COLS=4` に広げた。

`BRANCHTRY=1.00`（必ず試す）でも交差の決まりに阻まれて全部は引けず、
歩く道の上で **およそ 7割** に落ち着く。減らしたいときだけ下げる。

**事象の中身は段の順に決める。** 親と同じものは重みが半分になる
（`EVWEIGHT` を割る）。宝物の部屋（`treasure`）は `chest` と同じ扱いなので、
宝物の直後は事象の宝箱も半分。**親が2つとも同じでも半分は1回だけ**
（`forEach` で毎回割ると 1/4 まで落ちる）。

前は節の鍵の順に決めていたので、親の中身がまだ決まっていないことがあり、
続き具合を見ようがなかった。

### 世界地図（`worldMap(mode,pick)`）

`viewBox="0 0 100 100"` の SVG を組み立てて返すだけ。位置は `AREAS` の `x,y`。
y が大きいほど深い。左が森の道、右が水の道で、右下に水の帯を2枚重ねてある。
**名札は丸の下 `+5` と `+9.2` に出るので、`y` は 84 くらいまで。**
それより下だと viewBox からはみ出して切れる。

| mode | どこで | 何が光るか |
|---|---|---|
| `"home"` | ホームの世界地図 | 名前を知っている区画だけ明るい |
| `"run"` | 探索中の世界地図 | 通った道と現在地（白い輪） |
| `"gate"` | 入口を選ぶとき | 選べる区画が金色に脈打ち、丸を押しても選べる |

### 逃 走

1戦闘に1回だけ、1ターンを使って挑戦できる。`fleeUsed` が戦闘ごとの札で、
`prepareBattle()` で false に戻る。`foeStart` に開始時の敵の数を控えておく。

**判定は素早さの 1d100 対抗。** DEX の上限が 100 なので、下駄を履かせて
そのまま百分率として使える（`FLEEBASE=40`）。

```
こちら   = clamp(5,95, 40 + dexOf())
追っ手   = clamp(5,95, 40 + いちばん速い敵の DEX − 倒した数 × 12)
```

倒した数だけ包囲がゆるむ、という理屈で `FLEEDOWN=12` ずつ引く。
段階が高いほうが勝ち。同じ段階なら **出目の小さいほう**（より深く踏み込めたほう）が勝ち、
まったく同値なら振り切れない＝失敗。演出は索敵と同じ2段の `scRow` を使う。

**成功したとき**（`fleeBack()`）
- `RUN.path` の末尾（いま逃げてきたノード）を外し、ひとつ前へ `RUN.curKey` を戻す
- 戻った先の効き目は **もう得られない**。`enterNode()` を呼ばないので回復も宝も起きない
- ただし戻った先が焚き火なら **ものづくりだけはできる**（`craftModal(null)` のボタンを出す）
- 逃げたノードは `done` が立っていないので、そのまま選び直して入り直せる
- `RUN.path` が空になったら区画の入口。`reachable()` が `M.rows[0]` を返すので1段目から選び直し

**失敗したとき** は何も起きない。`resolvePlayer` がそのまま `runQueue()` へ落ちるので
1ターンぶんを敵に渡すことになり、札は「もう一度は試せない」で伏せられる。

`RUN.path` は `enterArea()`（区画に入るたび `[]`）と `stepTo()`（進むたび push）が面倒を見る。

### レベル

```
Lv1→2: 18   Lv2→3: 28   Lv3→4: 38 …（10ずつ増加、Lv25 の 258 まで）
```

1階層あたり10戦（通常7・精鋭2・ボス1）を想定。
アトランティス3階層でLv18、草原2階層でLv13前後。

**最初の戦闘で必ずLv2になる**よう18に設定してある。

### 技 能 値

能力値とは別の軸。**判定は幸運と同じ `roll100(pct)`。**

| 技能 | 持ち主になっている仕事 |
|---|---|
| `search` 探索 | 戦いのはじめの先制（`doScout`） |
| `shoot` 射撃 | 遠隔武器の命中（`threshold`） |
| `fight` 格闘 | 近接武器の命中（同上） |
| `forge` 鍛冶 | 装備をこしらえる（`doCraft`） |
| `cook` 料理 | 道具をこしらえる（同上） |

```js
skillOf(k) = SKJOB[職業][k] + SKRACE[種族][k] + SKORIG[欲望][k]   // 0〜95 で丸める
```

**能力値からも装備からもレベルからも来ない。** その人の心得。

#### なぜ入れたか ―― DEX の偏り

DEX は 命中・回避・行動順・索敵 の4つを一人で抱えていた。
STR と INT は「威力」だけ、VIT は「HP」だけなのに。
しかも DEX が伸びるのはアーチャーとスカウトだけ（`GROW` に DEX がある2職）。

命中を `shoot`/`fight` へ、索敵を `search` へ移し、**DEX に残るのは回避と行動順だけ**にした。

#### 遠隔と近接の違い

移す前、違いは **「後列を狙えるか」の1つだけ**だった（威力の式も命中の式も同じ）。
いまは射撃と格闘という別々の腕前があるので、
ナイトが弓を持てば当たらず、アーチャーが短剣を持っても当たらない。

#### 命中閾値（`threshold`）

**当てる側と避ける側で見る数字が違う。**

```js
if(u===me){
  t -= skillHit(wepSkill());                    // 技能 40 が並。25 ごとに 1 動く
  t += floor(相手のDEX / 15);                    // 速い相手ほど当てにくい
}else{
  t += floor((こちらのDEX - 敵のDEX)/15);         // 回避は DEX のまま
}
```

**相手の素早さは「そのまま」見る。**こちらの DEX との差では見ない。
差にすると自分の DEX がまた命中に効いてしまい、DEX から命中を剥がした意味が薄れる。
鈍足（`slow`）は相手の DEX を下げるので、そのぶん当てやすくなる
――これまで鈍足は行動順にしか効いていなかった。

| | スライム(6) | ゴブリン(20) | オオコウモリ(42) |
|---|---|---|---|
| ヒュームナイト（格闘70） | 3以上 | 4以上 | 5以上 |
| エルフウィザード（射撃40） | 4以上 | 5以上 | 6以上 |

同じ式が3箇所にコピーされていたのを `threshold()` 一本にまとめた。
コピーは `blur`（盲目）を読んでいなかったので、**敵を盲目にしても効いていなかった**。
まとめたついでに直った。

#### 当たる目の見せ方

3箇所に出す。**ボタンを描く時点では相手が決まっていない**ので、
狙える敵で幅があるときは `thrText()` が「5〜6以上」と範囲で返す。

| どこ | 何を出すか |
|---|---|
| 攻撃ボタン | `thrText()` |
| 技のボタン | `thrText({thr:s.thr, even:s.even}, 絞り込み)`。当てる技だけ |
| 相手を選ぶとき | `unitCell` の `.ctarget`。マスの右上に敵ごとの数字 |

「偶数の目」でしか当たらない技（`s.even`）は数字ではなく「偶数の目」と出す。
`.ctarget` は `pending` があるマスにだけ付く。

#### 索敵（`doScout`）―― 対抗判定

**こちらの探索と、相手の気配で 1d100 を振り合う。**
段階の差（−1〜4 にならしたもの）がそのまま先制ダイスになる（0〜3）。

```js
scoutHide(f) = (f.scout||0)*8 + floor(f.DEX/2)   // スライム 3 〜 オオコウモリ 69
scoutLevel(r) = r.fumble ? -1 : (r.crit ? 4 : r.k)
scoutBonus = clamp(0, 3, 自分の段階 - 相手の段階)
```

群れのときは **`scoutHide` がいちばん高い一匹**が代表して振る。

**片方だけが成功したときは、1ラウンド目だけ DEX を飛び越える。**

```js
scoutFirst = (a>0&&b<=0) ? "me" : (b>0&&a<=0) ? "foe" : null;
orderOf(first)      // "me" なら先頭へ、"foe" なら末尾へ
buildQueue()        // round===1 のときだけ scoutFirst を渡す
```

気配に気づいたほうが先に身構える、という理屈。
**両方成功・両方失敗のときは、これまでどおり DEX 順。**
2ラウンド目からは必ず DEX 順に戻る（`drawOrder` の「次」の表示も素の並び）。

`scoutFirst` は `prepareBattle` で null に戻す。

**画面には両者のダイスを並べて出す**（`.scRow`）。
名前・必要な目・出目・段階が横一列になるので、勝ったか負けたかがひと目で分かる。

*経緯：*
1. もとは両者が d6 を撒いて成功数の差を取っていた。自分のダイスが相手より
   桁違いに多く、**誰でも上限の +3 が出ていた**
2. 探索の 1d100 ひと振りに変えた。差はついたが、相手のぶんを数字にして
   こちらの技能から引いていたので、画面に出るのが `1d100 66／27` だけになり、
   **27 がどこから来たのか・勝ったのかが分からなかった**
3. いまの対抗判定にした

`me.scoutDice` は 2 の時点で削除した（表示だけの置物になっていたため）。

#### こしらえる（`doCraft`）

| 判定 | 結果 |
|---|---|
| ファンブル | 素材も加工賃も失う |
| 失敗 | 加工賃だけ失う。**素材は手元に残る** |
| 成功 | 料理1個 / 装備そのまま |
| ハード（上物） | 料理2個 / 装備 ×1.15 |
| エクストリーム（業物） | 料理3個 / 装備 ×1.3 |
| クリティカル（会心） | 料理4個 / 装備 ×1.5 |

**道具に品質を持たせられないのは `me.bag` が `{鍵:個数}` だから。**
だから料理の品質は「取れる数」で表す。装備は `me.eq` に実体が入るので
`gearWithQuality()` で数値そのものに倍率をかけ、名前に ＋/＋＋/★ を付ける。

#### 武器を打つ（職ごとに別物になる）

武器は**職ごとに等級表が分かれている**（`GEAR.wep[職]`）ので、
防具・装飾のように `make:"g_bone"` と id ひとつでは指せない。
レシピは **合印（`mk`）** だけを持ち、実体は `craftMake(R)` が
**いまの職の等級表から** `x.mk===R.make` で引く。

```js
f_beastwep:{kind:"forge",wep:true,make:"m_beast",need:{orcfang:3,claw2:2},cost:100}
// GEAR.wep.knight に {id:"wk_beast",key:"wildaxe",tier:2,made:true,mk:"m_beast"}
// GEAR.wep.archer に {id:"wa_beast",key:"savagebow",tier:2,made:true,mk:"m_beast"} …
```

**武器を足すときは4職ぶんそろえること。** 1つでも欠けるとその職では
レシピを覚えても何も作れない（`craftMake` が `null` を返す）。
`id` は職ごとに別にする（`owned` と `META.seen` が id で覚えるため）。
`made:true` を付けると `catPool` が宝箱・商人から外す。

**品質の効きは防具の4割（`WEPQ=0.4`）。**
武器が持っているのは威力の倍率ひとつだけで、そこに ×1.5 を掛けると
手数のぶんだけ効きすぎ、一等の打ち物が三等を軽く越えてしまう。

| | 防具・装飾 | 武器（`WEPQ` 後） |
|---|---|---|
| 上物 | ×1.15 | 威力 +6% |
| 業物 | ×1.3 | 威力 +12% |
| 会心 | ×1.5 | 威力 +20% |

倍率は等級表の側（`me.eq.wep.mul`）に書き、`WEP` の型そのものは書き換えない。
`recalcMe` が `m.wep={...WEP[key],mul:me.eq.wep.mul}` と写しを作る。

#### レシピの決まり（済）

**3つの決まりで組んである。足すときも崩さないこと。**

1. **出来上がりの等級 ＝ 要る素材のいちばん深い等級。**
   素材の等級は「いちばん浅く落ちる区画の階層」。
   ボスしか落とさないもの（王の粘液・巨人の眼・大樹の芯・守護核）は1段上。
2. **1つのレシピは1本の道の素材だけで完結させる。**
   森（原始の草原→木漏れ日の森→世界樹の森）と
   洞窟（原始の草原→苔むした洞窟→水没した回廊→アトランティス）で素材が交わらない。
   混ぜると片方の道でしか作れないレシピになる。草原の素材だけなら「共通」。
3. **打ったものはその等級で一番強い。** 品質（上物・業物・会心）でさらに上に伸びる。

いまの並び（30件。料理7は食材が宝箱と商人から出るので道に依らない）：

| 道 | 一等 | 二等 | 三等 |
|---|---|---|---|
| 共通 | 毛皮の胴着・猪牙の首飾り | ハイポーション | — |
| 森 | — | 樹皮の鎧・蔓の腕輪・大顎の篭手・獣追いの製法 | 花弁の衣・妖精の護符・疾走の蹄環・聖なる巨眼・世界樹の製法 |
| 洞窟 | — | 蝙蝠の外套・糸の手甲・討伐の証・糸と牙の製法 | 骨鎧・鱗鎧・石甲・爪の指輪・翼の護符・残滓の護符・守護核の冠・石打ちの製法 |

**鍛冶武器は道と等級で対になっていて、手数×威力が両者で揃えてある。**
片方の道を選んだせいで武器が弱い、ということが起きないようにするため。

| 職 | 森 二等 | 洞窟 二等 | 森 三等 | 洞窟 三等 |
|---|---|---|---|---|
| ナイト | ワイルドアックス 4.26 | ファルシオン 4.28 | ワールドブレード 4.86 | ストーンクラッシャー 4.86 |
| アーチャー | サベージボウ 5.19 | シルクストリング 5.20 | エルダーボウ 5.79 | アーバレスト 5.80 |
| スカウト | ビーストクロー 5.20 | ニードルファング 5.18 | ソーンエッジ 5.70 | ロックエッジ 5.72 |
| ウィザード | シャーマンスタッフ 4.41 | ボーンワンド 4.40 | ユグドラシルロッド 4.98 | グラニットロッド 4.98 |

森は手数を抑えて一撃を重く、洞窟は逆、という配り方にしてある（同じ数字でも当たり方が違う）。

**骨鎧・鱗鎧・残滓の護符はアトランティス（4階層）の素材を要る。**
四等の装備が無いので三等で止めてある。アトランティスの先の区画ができたら
この3つの等級を見直すこと。

#### 成功の段階（クトゥルフ神話TRPG 7版のやり方）

`lukRoll(pct)` が `{v, k, n, s, c, p, hit, hard, ext}` を返す。**`k` が段階（0〜3）**。

| 出目 | k | 段階 |
|---|---|---|
| `≦ 幸運 ÷ 5` | 3 | 超あたり |
| `≦ 幸運 ÷ 2` | 2 | 大あたり |
| `≦ 幸運` | 1 | あたり |
| それ以外 | 0 | はずれ |

**出目 1 は幸運がいくつでも必ず超あたり。出目 100 は必ずはずれ。**
段階は入れ子（超あたりの目は あたり の目でもある）なので、
幸運 20 なら 超4% ・ 大6% ・ 当10% ・ はずれ80%。

**効くのは2か所だけ。**

| | 効き目 | 場所 |
|---|---|---|
| ① 空振りの振り直し | 段階のぶんだけ振る（1〜3つ） | `playerAttack`（`!hits` の直後） |
| ② 宝箱 | `CHESTGAIN[k]` … 等級の上限・並ぶ数・金貨 | `openChest()` → `showChoice(...,lr)` |

```js
const CHESTGAIN=[
  {grade:0,pick:3,gold:1},      // はずれ
  {grade:1,pick:3,gold:1},      // あたり
  {grade:1,pick:4,gold:1.5},    // 大あたり
  {grade:2,pick:5,gold:2},      // 超あたり
];
```

② は**宝箱（`nd.t==="treasure"`）と打ち捨てられた箱（`ev==="chest"`）だけ**。
`showChoice(title,cats,0,out,lr)` の第5引数に `lukRoll` の結果を渡した所でしか働かない。
戦闘後の戦利品とレベルアップには効かない（**強欲の `extraPick` と丸かぶりするため**）。

`LUKGRADE` は `catPool("gear")` の `maxT` に足す一時値。`showChoice` が
`pickList` を呼び終えたら必ず 0 に戻す。戻し忘れると、その後の戦利品や
レベルアップにまで等級の底上げが漏れる。

**「人の業」（ヒュームのパッシブ `{k:"luck",v:1}`）とは別物。**
人の業は *必ず* 1つ、幸運は *段階のぶん* 0〜3つ。重なれば最大4つ振れる。

#### 地図の大きさ

```js
const ROWS=19, COLS=3, PATHS=4;   // 0段目からボスの手前まで
const TREASUREROWS=[4,11];        // 宝物を置く段
```

**ボスは `r:ROWS` に1つ置く**ので、1階層で通る部屋は `ROWS+1` 室。
いまは 19+1 で **20室目がボス**。

**通る道は1本なので、宝物は置いた段の数だけしか開けられない。**
以前は `r===8` の1段だけで、1階層に1回しか宝箱が出なかった。

| | 前 | 後 |
|---|---|---|
| 1階層で通る部屋 | 15 | **20**（20室目がボス） |
| うち宝物 | 1 | **2**（5室目と12室目） |

3階層通しで 45室 → 60室、宝箱は 3回 → 6回。

1周で通る内訳（300回平均）:
戦闘 7.6 / 事象 4.1 / 休息 2.3 / 宝物 2.0 / 精鋭 1.9 / 行商 1.1 / ボス 1.0

#### 宝箱の演出（`openChest(nd,weak)`）

`enterNode` の `treasure` と `resolveEvent` の `chest` は、どちらもここへ来る。

1. 全画面の `#chestOv` を出し、1d100 を回して段階を見せる
2. **中身と金貨をここで確定させる**（演出の途中で抜けても取りこぼさない）
3. 箱を `.shake` で揺らし、`.cFly` で中身を一つずつ飛ばす
4. 開いた箱（`chestFig("open")`）と一覧を見せ、ボタンで受け取る

**装備は選ばせない。**箱から飛び出したものをそのまま渡す。
`CHESTGAIN[k].gear` が数（1〜2）、`grade` が `catPool("gear")` の等級の上限。
枠が満杯だったものは `swaps` に溜めて、受け取ったあとに `swapModal` を鎖でつなぐ。

以前は 3〜5 択の `showChoice` に渡していたが、
**開けた手応えがそのまま報酬になるほうが素直**なのでやめた。

`chestSkip` を立てると `cwait()` が全部素通りして一覧まで飛ぶ（「とばす」ボタン）。
箱の絵は `PXCHEST.shut` / `PXCHEST.open`（16×16）。
金貨と覚え書きの絵は `PXITEM.i_gold` / `PXITEM.i_scroll` ―― **道具ではなく演出専用**で、
図鑑の件数（`Object.keys(ITEMS)` から数える）には入らない。

### 経済

```
                1階層   2階層   3階層
通常戦闘          16      22      28
精鋭              42      54      66
ボス              80     100     120
宝物              26      34      42
```

実際には ×0.85〜1.15 の揺らぎ、欲望の `goldMul`、パッシブ `gold` が乗る。
1周（3階層通し）でおよそ 960。

素材は荷物枠を使わない。道具の枠は基礎3（`BAGBASE`）＋欲望のボーナス。

### 上限

```
スキル      6   (SKMAX)
パッシブ   10   (PASSMAX)
道具        3 + 欲望のボーナス   (BAGBASE)
```

1周で17回習得できるので、終盤に必ず入れ替えが起きる。

---

## 4. コードの地図

`index.html` は 1枚の `<style>` と 1枚の `<script>` でできている。
スクリプト内はコメントの帯（`/* ===== … ===== */`）で区切られている。

### レイアウト

| 行 | 内容 |
|---|---|
| 1〜9 | `<head>`・フォント読み込み |
| 10〜555 | CSS 全部 |
| 556〜642 | `<section>` 8枚（画面の骨組み） |
| 643〜 | `<script>` 開始 |

### スクリプトの中身

| 行 | 区切り | 内容 |
|---|---|---|
| 646 | `VERSION` | 版。`tools/bump.js` が書き換える |
| 649 | `NEWSTAG` `NEWS` | お知らせ。新しいものを上に足す |
| 649 | `BGM` `BGMN` `Music` | 場面ごとの曲。切り替えは `showScreen` が引き受ける |
| 895 | `AUDIO` | 音量の設定。`audioLoad` / `audioSave` / `audioApply` |
| 730 | `SND` `SFX` | 効果音 12種。base64 の MP3・555 KB。`bus()` が音量のつまみ |
| 821 | ユーティリティ | `$` `r6` `wait` |
| 831 | `PX` | 立ち絵の部品（body/head/extra） |
| 981 | `PXFOE` | 敵51体のドット絵（16×20） |
| 1156 | `PXNODE` | 経路図のアイコン（12×12） |
| 1205 | `PXTAG` | 職業・種族・欲望・行動のアイコン28種（10×10） |
| 1307 | 立ち絵の組み立て | `meFig` `fig` `faceFig` |
| 1368 | `WEP` | 武器の型28種。職業ごとに7本ずつ |
| 1415 | `PXITEM` | 持ち物のアイコン21種 |
| 1464 | `PXMAT` | 素材のアイコン29種 |
| 1552 | `PXAIL` | 状態異常・強化のアイコン13種 |
| 1587 | `AIL` | 状態異常10種と `ailAdd` `ailSum` `ailTick` |
| 1661 | `ELEM` `ELCYCLE` | 属性10種と相性・`elemMul` |
| 1702 | `MATS` `FOODS` | 素材21・食材8 |
| 1739 | `RECIPES` | レシピ13件 |
| 1770 | `ITEMS` | 道具18種（うち料理6） |
| 1792 | `GEAR` | 武器の等級表24・防具9・装飾25 |
| 1847 | `RACE` `ORIG` `GROW` `JOB` | 種族4・欲望7・成長4・職業4 |
| 1908 | `REW` | スキル62・パッシブ67 |
| 2166 | `PCOND` | パッシブの発動条件14種 |
| 2193 | `FOE` | 敵51体 |
| 2345 | 状態 | `me` `foes` `sel`・`makeMe` `recalcMe` `makeFoes` |
| 2463 | 予告 | `chooseTele` `telePreview` — 敵の行動を2手先まで選ぶ |
| 2539 | 描画 | `draw` `unitCell` `drawOrder` `drawDetail` |
| 2676 | 判定 | `dieRate` `perHit` `powOf` `threshold` `defOf` |
| 2772 | 行動一覧 | `drawActs` — プレイヤーが選べる手を並べる |
| 2836 | ダイス演出 | 盤面の上に大きく出るやつ |
| 2943 | 攻撃の解決 | 実際にダメージが入る場所 |
| 3134 | プレイヤーの手番 | `doAct` |
| 3300 | 待ち行列 | `buildQueue` — DEX順の並べ替え |
| 3348 | 敵1体の行動 | `k` の種類ごとの分岐はここ |
| 3510 | `META` `ENCS` `AREAS` | 周回をまたぐ記録・遭遇68種・区画8種（うち3種は開発中） |
| 3646 | `genAct` | 経路の生成 |
| 3743 | `GOLD` `gainGold` `rollMats` | 金貨と戦利品 |
| 3838 | 進行 | `enterNode` `backToFloor` |
| 3921 | 地図の描画 | |
| 4022 | 行商の店 | `makeStock` `shopModal` `confirmBuy` |
| 4256 | 部屋の解決 | 休息・宝物・行商・事象の分岐 |
| 4433 | せり出す窓 | 状態画面。能力値・装備・スキル・道具・素材・レシピが1枚に入っている |
| 4561 | 報酬 | `EXPNEED` `SKMAX` `PASSMAX`・習得と戦利品の3択 |
| 4839 | 道具 | `BAGBASE` `bagMax` `useItem` |
| 4947 | 索敵 | |
| 4986 | 開始 | `prepareBattle` — 精鋭の倍率はここ |
| 5025 | 遊び方 | チュートリアル |
| 5055 | 見立て | 3問の診断 |
| 5124 | `PXDUN` | 区画の情景2種（24×14） |
| 5154 | キャラ作成 | 段階式の画面 |
| 5231 | ホーム／殿堂／図鑑 | |

---

## 5. データの件数

`node tools/probe.js` の出力（2026-09-02 時点）。

| | 件数 |
|---|---|
| 敵 | 51（うち2体は「蜘蛛の卵」「ヒールフラワー」で、組には入らず召喚でしか出ない） |
| 遭遇（敵の組み合わせ） | 140 |
| スキル | 62 |
| パッシブ | 67 |
| 武器の型 | 44（うち16は鍛冶でしか出ない） |
| 武器（等級表） | 40（4職 × 10。うち5本が鍛冶） |
| 防具 | 14 |
| 装飾 | 39 |
| 道具 | 20 |
| 素材・食材 | 40（素材32・食材8） |
| レシピ | 33（料理8・鍛冶25） |
| 職業 / 種族 / 欲望 | 4 / 4 / 7 |
| 属性 / 状態異常 | 10 / 11 |
| 区画 | 8（うち2つは開発中で降りられない） |
| 効果音 / BGM | 12 / 10 |

### 図鑑（370件）

```
モンスター51 / スキル62 / パッシブ67 / 武器44 / 防具14
装飾39 / 道具20 / 素材40 / レシピ33
```

**すべて発見制**。実際に手にしたものだけ記録される。
選択肢に出ただけでは記録されない。

### モンスターの段階（`CODEXN`）

`META.codex[k]` は**その敵と戦った回数**。同じ敵が3体並ぶ遭遇でも 1戦で 1 しか増えない。

```
1段目   1戦   名前と列
2段目   3戦   HP・攻　＋命中閾値 −1
3段目   6戦   DEF・速　＋命中閾値 さらに −1
4段目  10戦   使う技の名前
5段目  15戦   弱点と耐性（突いていなくても分かる）
```

閾値は `CODEXN=[1,3,6,10,15]` の1行。**遅くしたい・段階を増やしたいときはここだけ触る。**
`codexLv()` が回数から段階を出し、`codexNext()` が次まであと何戦かを返す。

弱点は**突けばいつでも**記録される（`learnElem`）。5段目は「突かなくても分かる」という
別の入口で、`knownElem(key,kind)` が両方をまとめて答える。
表示する側（戦闘中の敵の詳細・地図の下調べ・図鑑）は必ずこれを通すこと。

**前はこうだった。** `META.codex[k]` に**体数**をそのまま足し、段階＝目盛りだった。
3体そろう遭遇を2回こなすだけでカンストし、実測で草原1階層（10戦）が終わる前に
主要な敵が全部 5/5 になっていた。5段目には何も無かった。

1周（草原2階層・20戦）でどこまで進むかの実測：

| 敵 | 戦った回数 | 段階 |
|---|---|---|
| オーク・ウルフ・マンドラゴラ・ホーンラビット・ゴブリン | 11〜13戦 | 4/5 |
| ワイルドボア・ワイルドベア・トレント・マンイーター | 6〜7戦 | 3/5 |
| スライム | 5戦 | 2/5 |
| ボス（キングスライム・サイクロプス）| 1戦 | 1/5 |

**5段目は1周では埋まらない。** 何周かかけて埋めるもの。
記録は `localStorage` に残るので、ページを閉じても続きから進む（§6「記録の保存」）。

### 3軸とプール

| | スキル | パッシブ |
|---|---|---|
| 共通 | 4 | 6 |
| 職業 ×4 | 各7 | 各5 |
| 種族 ×4 | 各2 | 各5 |
| 欲望 ×7 | 各3 | 各3 |
| **1人が引ける** | **16** | **19** |

---

## 6. 拡張のしかた

### 世 界 の 決 ま り ご と

物語の設定。**敵や区画を足すときは、まずここに矛盾しないか確かめること。**

#### 世界の骨組み

- **地上に人間の社会がある。** ゴブリンの耳が「討伐の証として取引される」＝討伐依頼を出す側がいる。
  冒険者は そこから降りてくる
- **世界樹**は 大量のマナを放つ超巨大な木。森の道の奥にある。
  トレントが動き出すのも、魔法の森が生まれたのも、このマナのせい
- **アトランティスが沈んだのは世界樹とは別件。** 二つの道は繋がっていない。
  何が沈めたのかは まだ決めていない
- **守護核** … 「番をするものの中心にあったもの」。**落とすのはボスだけ**。
  ゲートガーディアン（水没した回廊）と ディープワン（アトランティス）の2体。
  苔むした洞窟の主は クイーンスパイダー で、ゴブリンシャーマンは精鋭に回っている

#### 区画

| 区画 | どういう場所か |
|---|---|
| 原始の草原 | 地上のすぐ下。**野生動物と粘体だけ**。亜人はいない |
| 木漏れ日の森 | 世界樹のマナが薄く届く森。植物が動き出している |
| 世界樹の森 | 世界樹の根方。陽は届かず、マナだけが濃い。妖精と、マナを浴びて動き出した木々 |
| 魔法の森 | 世界樹のマナが流れ込んで生まれた魔素の濃い森。独特の生態系（**中身は未定**） |
| 苔むした洞窟 | 外縁の掘削跡。**人の手が入った最後の場所**。**蜘蛛が先に住んでいて**、ゴブリン一族と分け合っている |
| 水没した回廊 | 切り出した石が並ぶ。**作られたものが番をしている** |
| 水没都市アトランティス | かつて栄えた地下都市。**住人だったものが残っている** |
| 夜空の洞窟 | 天井に **星石（ルナストーン）** が星空のように光る。魔道具（ランタンなど）の燃料になる。その影響で独特の生態系と植生（**中身は未定**） |

#### ボスだけが落とす素材

**素材の等級は「いちばん浅い区画」で決まる。ただしボスしか落とさないものは1段上。**
レシピの等級（作れるもの）は、要る素材のいちばん深い等級に合わせる。

| ボス専用の素材 | 落とす相手 | 実質 |
|---|---|---|
| 王の粘液 | キングスライム（1階層） | 二等 |
| 巨人の眼 | サイクロプス（2階層） | 三等 |
| 大樹の芯 | エルダートレント（3階層） | 三等（四等が無いので上限） |
| 守護核 | ゲートガーディアン（3階層）・ディープワン（4階層） | 三等（同上） |

**それ以外はすべて通常か精鋭でも落ちる**ので、区画の階層がそのまま等級になる。
精鋭でしか落ちない素材は いまのところ無い（作るなら ボスと同じ扱いにするか
決めること）。

4階層（アトランティス）の素材 — 古い骨・魚人の鱗・残滓 — は
**四等の装備ができるまで宙に浮いている**。骨鎧・鱗鎧・残滓の護符が
これを要求しているので、アトランティスの先の区画ができたら等級を見直す。

#### 部屋の重さの並べ方

`solo`／`easy`／`norm`／`hard`／`elite` の5つの枠は
**1ターンあたりの被害（被/T）の帯で分ける。**帯どうしを重ねない。

- **同じ遭遇を2つの枠に入れない。**精鋭は倍率（HP×1.4・STR×1.15）が
  かかるので数値は上がるが、顔ぶれが「重い」と同じだと部屋が繰り返しに見える
- **各枠 最低2件。**件数はそろえなくてよい
- 帯の例（原始の草原）… 単体 12〜23／軽い 24〜29／普通 36〜48／重い 53〜59／精鋭 67〜76

測り方は `tools/` ではなくその場のスクリプトで、**ゲーム本体の
`powOf`／`perHit`／`defOf`／`depthMul` を呼んで**出す。
式を写すと必ずずれる（`tools/balance.js` で2度やった）。

    被/T ＝ Σ（行動の重み ÷ 全行動の重み × ダイス数 × perHit × 0.55）

`0.55` は命中のおよその率。毒は `poison × 1.5` で足す。

#### 同じ敵を2つの区画に出さない

**階層が違うなら、その敵はどちらか一方にだけ出す。**

スライムとゴブリンが 原始の草原 と 苔むした洞窟 の両方に出ていたのは、
はじめ**その2つを どちらも第一階層として設計していた名残**で、
階層が分かれたあとも残っていたもの。

- ゴブリンは 苔むした洞窟 へ一本化した（説明文も「洞窟に住み着く」と書いてある）
- 草原から抜けた5つの遭遇は、獣と粘体で組み直した（`p_wr` `p_gw2` `p_wr2` `p_bs` と `p_hard` の作り直し）

これで**どの素材が何区画目で採れるかが一意に決まる**。
レシピの等級（要る素材のいちばん深い区画。ただしボス素材は1段上）は
これが決まっていないと引けない。

**同じ敵を2区画で使いたくなったら、それは別の敵として立てること。**

#### 族と、そろえてある値

**族ごとに 属性・弱点・耐性・素材をそろえる。** ばらばらに決めない。

| 族 | いるもの | 属性 | 弱点 | 耐性 | 素材 |
|---|---|---|---|---|---|
| 獣 | ホーンラビット・ウルフ・ワイルドボア・ワイルドベア | 無 | 個別 | 個別 | 角・毛皮・牙・爪 |
| スライム族 | スライム・草スライム・水スライム・キングスライム | 無（水スライムとキングだけ水） | 雷 | 叩 | 粘液 |
| 亜人族・ゴブリン | ゴブリン・弓ゴブリン・ホブゴブリン・ゴブリンキャスター・ゴブリンシャーマン | 無（術者だけ闇） | 斬〜貫 | — | ゴブリンの耳（シャーマンだけ守護核＝奪ったもの） |
| 亜人族・大型 | オーク・サイクロプス | 無〜土 | 斬・貫 | 叩 | オークの牙・巨人の眼 |
| 植物 | マンイーター・マンドラゴラ・トレント | **土** | **火** | 個別 | 蔓・根・樹皮 |
| 石の造物 | ゴーレム・ガーゴイル・ゲートガーディアン | 土 | 叩 | 斬 | 石の羽・守護核 |
| 擬態する虫 | リビングストーン | 土 | 叩 | 斬 | 石片 |
| 不死 | スケルトン・スケルトンアーチャー・レイス | 闇 | 光・叩 | 貫・斬 | 古い骨・残滓 |
| 魚人 | サハギン・ディープワン | 水 | 雷 | 水 | 魚人の鱗・守護核 |
| 妖精 | ウィスプ・スプリガン・レッドキャップ | 光/土/闇 | 個別 | 個別 | マナの雫・妖精の粉 |
| マナの木々 | ドライアド・アルラウネ・世界樹の苗・エルダートレント | 土 | 火 | 個別 | 大輪の花弁・マナの雫・大樹の芯 |
| 森の獣人 | ケンタウロス・ハーピー | 無 | 貫 | 個別 | 硬い蹄・風切り羽 |

**リビングストーンは造物ではなく、岩に擬態する虫。** 見た目と属性は石の造物と
そろっているが、生きものなので族が違う。素材の「石片」は ゴーレムと共用なので、
説明は **どちらにも当てはまる書き方**にしてある（「動いていたものから剥がれた石」）。
造物の瓦礫にも、虫の殻にも読める。

#### 説明文（`FOE[k].d`）

敵ひとりずつに設定を持たせてある。図鑑には**1戦でも戦えば**出て、
戦闘中の敵の窓にも出る。数値ではなく **その敵が何者か** を書く。

**書き方は「生物図鑑」。** 三つの文でひとまとまりにする。

```
① 見た目      … 何であるか、体のどこが特徴か
② 生態・習性  … どう暮らし、どう動くか。観察した口ぶりで
③ こちらへの意味 … 危ないのか、素材として使えるのか
```

> **ホーンラビット**
> 草原のどこにでもいる、頭に一本の角を持った兎。
> 足がかなり速く、油断していると その角で腹を貫かれる。

**やってはいけない書き方** … 数値の言い換え（「HPが高い」「攻撃が重い」）、
遊び方の説明（「先に倒すとよい」は ③ に混ぜる程度にとどめる）、
決めていない設定への言及（アトランティスが沈んだ理由など）。

**新しい敵を足すときは `d` を必ず書くこと。** 上の族の表に当てはめ、
当てはまらないなら族のほうを先に決める。

### 敵を足す

```javascript
FOE.newfoe = {
  n:"名前", mat:"素材キー", weak:"blunt", resist:"slash", elem:"earth",
  art:"newfoe", snd:"blunt",
  HP:120, STR:33, DEF:8, DEX:20, rank:"front", scout:3,
  acts:[
    {n:"殴る", k:"atk", dice:3, w:5},
    {n:"構える", k:"guard", block:14, w:3},
  ],
};
PXFOE.newfoe = [[x,y,w,h,"#色"], ...];   // 16×20 のドット絵
ENCS.new1 = {n:"遭遇名", list:["newfoe","newfoe"]};
```

`AREAS[x]` の `solo/easy/norm/hard/elite/boss` に遭遇キーを足せば出現する。
足したら `node tools/probe.js --check` を通す。参照の取りこぼしを拾ってくれる。

**行動の種類**

| `k` | 内容 |
|---|---|
| `atk` | 攻撃。`dice` `pow` `drain` |
| `guard` | 自分のDEF上昇（1ターン） |
| `buff` | 自分の威力上昇。`dur` で持続 |
| `cheer` | 前列全体の威力上昇 |
| `veil` | 前列全体のDEF上昇 |
| `hex` | プレイヤーを弱体化。`curse` `weak` `slow` `dice` `blind` `dur` |
| `regen` | 自分のHP回復 |
| `summon` | 仲間を呼ぶ。`who` `cd` `once` `alone` |

`w` は選ばれやすさの重み。

**特殊な指定**

- `seq:1` — 決まった手番でのみ使う
- `solo:true` — 味方が絶えたときだけ使う
- `needFront` / `noFront` — 前列の有無で条件分岐
- `pos:"back"` — 自分がその列にいるときだけ
- `forceNext:"技名"` — 次に必ずその技を使う
- `flex:true`（敵本体）— 前列・後列にランダム配置

### スキルを足す

```javascript
REW.act.knight.push({
  id:"ak8", n:"技 名", kind:"atk", mp:4, cd:3,
  powMul:1.25, el:"slash", ail:[{k:"burn",v:10,t:2}],
  d:"説明文",
});
```

`kind` は `atk` `guard` `buff` `heal` `breath` `focus` `declare` のいずれか。
`id` は図鑑が発見を記録する鍵なので、既存とかぶらせないこと（`--check` が重複を見る）。

**攻撃の修飾子**

`dice` `fixDice` `powMul` `thr` `six` `chain` `pen`（DEF無視率）
`all`（列全体）`allFoes`（全体）`column`（縦貫通）`pierce`（列2体）
`backOnly` `slower` `toBack` `even`（偶数命中）`again`（追撃）
`lifeSteal` `devour` `steal` `jealous` `rage` `lostDice` `lostPow`
`mpDrain` `loot` `charm` `earlyOnly`

### パッシブを足す

```javascript
REW.pass.common.push({
  id:"p7", n:"名 前", ef:{k:"powp", v:12, w:"hp50"}, d:"説明文",
});
```

`k` の主なもの：`powp`（威力%）`guard` `dice` `thr` `six` `dex` `scout`
`maxhp` `maxmp` `maxmpp` `pen` `luck` `feed` `blood` `fixate` など。

`w` は発動条件。`PCOND`（2166行）に定義されている14種。

```
always  r1  hp70  hp50  hp30  hp80  hpfull  mp50  mpfull
front  back  def10  guarded  solo
```

新しい条件を足すなら `PCOND` に関数を追加する。
`--check` は `PCOND` に無い条件を指しているパッシブを弾く。

### 区画を足す

`AREAS` に1つ足して、どこかの `to` から指す。中身がまだ無いなら `wip:true` を
付けておく（地図には「開発中」と出るが、降りられない）。

```javascript
AREAS.newarea = {
  n:"名 前", tier:3, art:"plains",       // art は PXDUN の鍵
  x:40, y:60, land:"forest",             // 世界地図の位置。grass/forest/cave/water
  d:"説明",
  to:["つぎの区画のキー"],
  start:"はじめての潜行",                 // 出発点にできる区画だけ
  solo:["s_x"], easy:["e1"], norm:["n1"], hard:["h1"],
  elite:["h1"], boss:"b1",
};
PXDUN.newarea = [[x,y,w,h,"#色"], ...];  // 24×14 の情景（art を新設するなら）
```

`node tools/probe.js --check` が
遭遇キーの実在・`to` の行き先・どこからも辿り着けない区画・
`wip` なのに遭遇が入っている区画 を見てくれる。

### BGMを足す

ファイルを `audio/bgm/` に置き、`BGM` に場面のキーで足す。
`BGMN` に短い曲名を書いておくと設定画面に出る。

```javascript
const BGM={
  home  :"遥かなる伝説.mp3",           // ホーム・キャラ作成・遊び方・見立て
  hall  :"そろそろ帰ろっと！_M325.mp3",    // 殿堂
  result:"そろそろ帰ろっと！_M325.mp3",    // 潜行の結果
  codex :"オルゴールファンタジー.mp3",     // 図鑑
  boss  :"深域で待つもの.mp3",           // ボス戦
  plain :"…",                        // 探索は AREAS の鍵そのもの
  hallw :"…",                        // ただし hall だけは読み替える（下）
};
```

**場面と曲は 1 対 1。**1つの区画に複数の曲を持たせる仕組みは無い。

**`hall` という綴りが2つある。**画面の `hall`（殿堂）と
`AREAS.hall`（水没した回廊）が同じ鍵で、`BGM.hall` は殿堂のものだった。
そのため**水没した回廊に降りると殿堂の曲が鳴っていた**（気づかれずにいた）。
`bgmKey()` が区画の `hall` だけを `hallw` に読み替えることで分けている。

```javascript
const area=()=>RUN?(RUN.area==="hall"?"hallw":RUN.area):"home";
```

**区画の鍵を足すときは、画面の鍵（home・hall・codex・result・boss）と
ぶつかっていないか必ず見ること。**ぶつかると無言で別の曲が鳴る。

**曲を切り替えるのは `showScreen()` の1箇所だけ。**
どの場面にどのキーを使うかは `bgmKey()`（3730行あたり）が決める。

```
fight  → RUN.boss なら "boss"、そうでなければ探索と同じ曲
floor  → RUN.over なら "result"、そうでなければ 区画のキー
hall   → "hall"      codex → "codex"
その他 → "home"（home・make・tut・quiz）
```

新しい場面に曲を足すなら `bgmKey()` に1行足す。個別に `Music.play()` を
呼ぶ必要はないし、呼ばないほうがよい（切り替えの入口を1つに保つため）。

**キーではなくファイル名で比べている。** 殿堂と結果のように曲が同じ場面をまたぐとき、
頭から鳴り直さずそのまま続く。

音源はループ前提のものを置くこと（`<audio loop>` で繋いでいる）。
日本語のファイル名は `encodeURI()` を通しているのでそのままでよい。

### 外から読む効果音を足す

合成（`SND` と `tone`／`noise`）では出せない音だけ、mp3 を外から読む。
ファイルを `audio/sfx/` に置き、`SFXFILE` に足す。

```javascript
const SFXFILE={
  win:"jajan.mp3",       // 討伐したとき
};
```

鳴らす側は **必ず合成の音に落とせる形で書くこと。**

```javascript
win(){ if(shot("win"))return;            // mp3 が鳴ればそれで終わり
       [523,659,784,1047].forEach(…); }  // 無ければ合成の和音
```

`shot()` は読めなかった時点で `bad` の印を付け、二度と試さない。
毎回 404 を出さないための作りなので、印を消す道はわざと用意していない
（ファイルを置いたら読み込み直すこと）。
自動再生を止められた場合は `now` を戻し、次に何か押されたとき
（`document` の click から `Music.resume()`）鳴らし直す。

### 記録の保存

`META` を `localStorage` の `dd.meta` に置く。1周ごとに作り直すキャラの外側に
残るもの（図鑑・殿堂・弱点の記録・到達階層・遊び方を見たか・お知らせをどこまで読んだか）
がここに入る。

```javascript
const MKEY="dd.meta";
const MVER=1;       // 中身の形を変えたら上げる。読み込み側で移行する
const HALLMAX=50;   // 殿堂は増え続けるので上限。古いものから捨てる
```

**記録を変えたら `touchMeta()` を呼ぶこと。** 400ms まとめてから書く
（1戦で何度も呼ばれるため）。呼ぶ場所は9箇所。

```
learnElem          弱点・耐性を覚えた
rollMats / gainFood / lootModal   素材を手にした
startRun           潜った回数
enterNode          訪れた部屋
markSeen           何かを発見した
nextStage          図鑑の目盛り
tutOK              遊び方を見た
endRun             殿堂　← ここだけ metaWrite() で即座に書く（1周の終わりなので）
```

閉じられる前に `pagehide` と `visibilitychange` で `metaFlush()` する。

**保存が使えなくても遊べるようにしてある。**
プライベート窓・容量切れ・site data を切っている場合、`metaBroken` が立ち、
以降は保存を諦めて動き続ける。設定画面がその旨を出す。

読み込み側は疑ってかかる。JSON が壊れている・`v` が違う・型がでたらめ、
どの場合も既定値に落として遊べる状態にする（`metaLoad`）。

記録を消す道は設定画面にある（2度押しで確定）。
**遊び方を見たか・お知らせを読んだかは消さない。**
消すと毎回チュートリアルに戻され、読んだお知らせがまた未読になって煩わしい。

`META` に項目を足すときは `metaLoad` の読み取りと `metaWrite` の書き出しの
**両方**に足すこと。片方だけだと黙って消える。
既存の保存に無い項目は既定値に落ちるので、`MVER` を上げる必要はない。

### 潜行の保存（つづきから）

`META`（周をまたぐ記録）とは別に、**いま潜っている途中の一周**を
`localStorage` の `dd.run` に丸ごと控える。家に「つ づ き か ら」が出る。

**控える瞬間は3つ。**

| いつ | どこ | なぜ |
|---|---|---|
| 部屋に踏み込む直前 | `stepTo()` の頭 | 戦闘中に閉じても **その戦闘の開始前**から続けられる |
| 部屋を抜けて地図に戻ったとき | `backToFloor()` | 買い物・こしらえた物を落とさない |
| 新しい区画に入ったとき | `enterArea()`（1区画目を除く） | 区画をまたいでも続く |

1区画目だけ `enterArea` で控えないのは、そのあと「出立の支度」の窓が出るため。
選ぶ前に控えると装備なしの姿で続きが始まってしまうので、
支度が済んだ `startPick(0)` で控える。

**控えるもの** … `sel`（職業・種族・欲望・区画・幸運）、`me` まるごと、`owned`、
`RUN` の `act/area/seen/curKey/path/M/stock/shopRecipe`。だいたい 5〜7KB。

`RUN.cur` は `RUN.M.node` の中身を指しているだけなので控えず、
読み戻すときに `curKey` から繋ぎ直す。`RUN.preview`・`RUN.lootLines` は落とす。
データ表に関数は1つも入っていないので `JSON.stringify` で往復できる
（`tools/probe.js` の書き出しを走査して確かめてある）。

**捨てる瞬間** … `endRun()`（踏破でも戦死でも）と、
家で「新しく作って潜る」を選んだとき（断りの窓が出る）。

**読めるかどうかまで見る**（`runLoad`）。表の中身が変わって
職業・種族・欲望・区画・遭遇の鍵のどれかが消えていたら、その控えは捨てる。
`RVER` は控えの形そのものを変えたときに上げる。

#### クラウドと突き合わせる

控えは `META` と同じ書類（`games/dicedungeon/players/{uid}`）の `run` に載る。
`runSave()` が `CloudHook.mark()` を打ち、間を置いてまとめて送る。

潜行には `RUN.id` という目印がある（`startRun` で作る）。
これで「同じ潜行の進み具合の違い」と「別々の潜行」を見分ける。

`sync()` が `runMerge(cloud)` を呼び、こうする。

| | どうするか |
|---|---|
| 片方しか無い | あるほうを採る |
| 同じ潜行（id が同じ） | **新しいほう**（先へ進んでいるほう）を採る |
| 別々の潜行（id が違う） | **どちらを残すか選ばせる**（`runPickModal`） |

**潜っている最中（`RUN && !RUN.over`）は何もしない。** 遊んでいる手元の控えが正しい。
クラウドが `run: null` でも手元の控えは消さない。
つながっていない間に潜っていた、という筋があるため。

#### 終わった潜行が生き返らないように

1台で潜行を終えても、もう1台には古い控えが残っている。そのままだと
そちらで「つづきから」に出て、終わったはずの潜行をもう一度遊べてしまう。

`endRun()` が `META.ended[RUN.id]=true` の印を置く。これは `META` に乗るので
`metaMergeFlag` でもう1台にも届く。`runLoad()` は印のある id の控えを読まない。
印は `ENDEDMAX=30` 件で頭から捨てる。

`firestore.rules` の鍵の並びに `run`（書類の側）と `ended`（meta の側）を
足してあるので、**ルールを配り直すこと**（`firebase deploy --only firestore:rules`）。

### 記録をクラウドに置く（Firebase）

設定のしかたは **`FIREBASE.md`**。ここには仕組みだけ書く。

**端末内が主、クラウドはその写し。** `FIREBASE_CONFIG` が空でも、通信が死んでいても、
`file://` で開いていても、ゲームはこれまでどおり動く。**この一線は崩さない。**

```
index.html
  FBVER            読み込む Web SDK の版。CDN は gstatic
  FIREBASE_CONFIG  コンソールから貼る。空ならクラウド保存は眠ったまま
  CKEY "dd.cloud"  覚えるのは「自分でサインアウトしたか」だけ
  CloudHook        metaWrite/metaClear から呼ばれる差し込み口。既定は空関数
  Cloud            SDK の遅延読み込み・サインイン・同期
  metaMerge(r)     取ってきたものを META に混ぜ込む（クラウドとは無関係の純関数群）
```

| | |
|---|---|
| 置き場所 | Firestore `games/dicedungeon/players/{uid}` に `{v, meta, at}` の1件（`CPATH`） |
| ログイン | 起動時に黙って匿名。あとから `linkWithPopup` で Google に紐付ける |
| 書き込み | `metaWrite()` のたびに予約し、8秒ぶんまとめて送る |
| 必ず送るとき | 1周の終わり（`metaFlush`）・記録を消したとき・画面を閉じる直前 |
| 守り | `firestore.rules`。**ブラウザ側のコードは守りにならない** |

**SDK は `import()` で使うときに取りに行く。** `<script type="module">` にすると
`file://` で全部動かなくなるので、classic script のまま動的読み込みにしてある。

**置き場所に一段名前を挟んである**（`users/{uid}` ではなく `games/dicedungeon/players/{uid}`）。
同じ Firebase プロジェクトを別のゲームと相乗りさせたときにぶつからないため。
`firestore.rules` はプロジェクトに1つしか置けず deploy が丸ごと差し替えるので、
相乗りさせるならルールは貼り足す（`FIREBASE.md` §8）。

#### 統合のきまり（`metaMerge`）

2台で別々に遊んでいることがある。**どちらかを捨てない。**

| | |
|---|---|
| `runs` `best` | 大きいほう |
| `codex` `rooms` | 鍵ごとに大きいほう（戦った数） |
| `mats` `seen` `areas` | 片方でも印が付いていれば付く |
| `elem` | 敵ごとに `{weak,resist}` を混ぜる |
| `hall` | 両方を混ぜ、`t`（記録した時刻）で重複を落とし、新しい50件 |
| `newsSeen` | `NEWS` の並びで新しいほう |

殿堂の一件に `t:Date.now()` を打つようにしたのはこのため。
`t` が無い古い記録は JSON そのもので重複を見る。

#### サービスアカウントキーは置かない

`firebase-admin` の `serviceAccountKey.json` は**全データを無条件に読み書きできる鍵**で、
セキュリティルールを一切通らない。このゲームは静的サイトなので、
そこに置けばソースを開いた誰でも全ユーザーの記録を消せる。
`.gitignore` で弾いてある。要るのはサーバー処理（Cloud Functions）を足すときだけ。

### お知らせ（封筒）

右上、歯車の左隣。`NEWS` に新しいものを**上に**足していく。

```javascript
const NEWS=[
 {id:"v0005",tag:"up",v:"α0.0005",d:"2026-08-30",n:"見出し",b:["本文","本文"]},
 …
];
```

**`id` は一度決めたら変えないこと。** どこまで読んだかを `META.newsSeen` に
その id で覚えている。変えると全員の既読が巻き戻る。

`tag` は `NEWSTAG` に定義（`up` アップデート / `fix` 修正 / `bal` 調整 / `info` お知らせ）。

**版を上げるときは、必ず `NEWS` にも足すこと。**
α0.0014 から α0.0025 までの12版ぶんを書き忘れ、
ワールドマップ・逃走・つづきから・装備画面の作り直しまで、
遊ぶ人には一切知らされていなかった。
`tools/bump.js` を叩いたら `NEWS` の先頭に1件、が対になる。
増やしたければそこに足す。`b` の各行は HTML をそのまま書ける（`<b>` で強調）。

未読の数は `newsUnread()`。`NEWS` は新しい順なので、`META.newsSeen` の位置より
前にあるものが未読。封筒を開いた時点で既読になる（印は先に描いてから記録する）。

**版を上げたら、その版のお知らせも足すこと。** `tools/bump.js` は
`VERSION` しか触らないので、`NEWS` への追記は手作業。

### 音量の設定

右上の歯車（`#gear`）を押すと出る。`AUDIO`（895行あたり）が持ち、
`localStorage` の `dd.audio` に残る。読めない環境でも既定値で動く。

```
AUDIO = {master:100, sfx:80, bgm:50}      // 0〜100

実際のBGM音量   = master/100 × bgm/100    → <audio>.volume
実際の効果音音量 = master/100 × sfx/100    → SFX の bus() の gain
```

効果音は `SFX` の中の `bus()` が返す GainNode を全部が通る。
個々の音の大きさ（`play("click",.55)` の .55 など）はそのままで、
つまみはその後ろの1つのつまみを動かす。

`♪ 音 ON/OFF` のボタンは戦闘画面にしかないので、
消音中につまみを動かすと音が戻るようにしてある（`soundToggle()`）。

---

## 7. 既知の不具合

### 〔修正済〕敵の `hex` が何もしていなかった

`hex` は `me.curse` と `me.weak` に値を足していたが、**それを読む計算がどこにも無く**、
ログと画面表示だけ出して数字を一切動かしていなかった。対象は敵の行動11件。
`dur` の持続処理も無く、`slow` と `dice` の指定も読まれていなかった。

状態異常（`me.ail`）に載せ替えた。

```
curse:N  → ailAdd(me,"break", N)        DEF −N
weak:N   → ailAdd(me,"weakp", N*15)     威力 −N×15%（前の表示に合わせた）
slow:N   → ailAdd(me,"slow",  N)        DEX −N
dice:N   → ailAdd(me,"shrink",N)        攻撃ダイス −N
blind:N  → ailAdd(me,"blur",  N)        命中閾値 +N
```

`dice` のために状態異常「萎縮」（`shrink`）を足した。攻撃ダイスの計算
（`doAtk` の `let n=...`）で `ailSum(me,"shrink")` を引く。

同じ弱体を重ねがけしても増えない（`ailAdd` が強いほうを採って上書きする）。

### 〔修正済〕弱体の器が3通りに散っていた

`me.ail` / `me.debuffs` / `me.curse`+`me.weak` の3通りがあり、後ろ2つは
書く側と読む側が繋がっていなかった。`me.ail` に一本化した。

- `addDebuff()` `tickDebuffs()` `me.debuffs` を削除（`addDebuff` は呼び出し元が0だった）
- `curseSum()` `weakSum()` を削除。`slowSum()` は `ailSum(me,"slow")` の別名として残した
- `powOf()` のプレイヤー分岐を敵と同じ形にした
  （固定値 `weak` を引いてから割合 `weakp` を掛ける）。
  前のプレイヤー用の分岐はパッシブの倍率を素通りしていた
- `defOf()` は敵と自分の両方で `ailSum(u,"break")` を引く

**弱体を足すときは `ailAdd()` を使うこと。別の入れ物を作らない。**

### 〔修正済〕空振りの振り直しが一度も働いていなかった

「人の業」の振り直しは `playerAttack` の**的ごとの判定の中**に書いてあった。
ところがその手前に

```js
if(!hits){ ... 空振り ... return; }
```

があり、**一つも当たらないと必ずここで抜けていた**。
`vals`（的ごとの当たり）が空になるのは `hits` が 0 のときだけなので、
ふつうの攻撃では振り直しの行に一度も到達していない。
（例外は「偶数の目だけ当たる」技。当たりはあっても偶数が無いことがある）

振り直しを**早期 `return` の手前**へ移した。的ごとの判定に残したのは
`opt.even` のときだけで、`rerolled` で二度振りを防いでいる。
幸運（§3）の振り直しも同じ場所に入れてある。

### 〔修正済〕`me.order` がどこからも設定されていなかった

`dexOf()` に `me.order ? Math.round(me.DEX*me.order.dex/100) : 0` という枝があり、
`me.order` を読む場所は2つあったが **書く場所がどこにも無かった**。
常に 0 として素通りしていた。消した。

「書いてあるのに繋がっていない」はこれで4件目
（`Music.play` / `hex` / `addDebuff` / `me.order`）。
**新しい枝を足したら、必ず呼び出し側があるか確かめること。**

### 〔修正済〕打ち捨てられた箱の金貨が表示と違った

`const g=Math.round(gainGold("treasure")*0.8);` と書いてあったが、
`gainGold()` は**呼んだ時点で満額を `me.gold` に足している**。
0.8倍したのは画面に出す数字だけで、財布には満額が入っていた。
`openChest()` では倍率を出したあと差額を `me.gold` に足し直している。

### 〔修正済〕アンチポーションが置物だった

`me.debuffs` を空にするだけで、それが常に空だったので何も解いていなかった。
**状態異常をすべて解く**ように書き直した（毒・火傷・麻痺・盲目・混乱を含む）。

### 状態異常の決まり（決定済み）

**状態異常は戦闘の中だけのもの。戦闘をまたがない。**

`finish()` で `me.ail=[]` する。探索画面では必ず空。

またがせない理由。

1. **持続をターン数で測っている。** `ailTick()` と `ailDot()` が動くのは
   その者の手番の頭だけ。探索中は手番が無いので、持ち越すと持続が
   一切減らない。「3ターンの毒」が次の戦闘まで残り、そこから3ターン続く。
   またがせるなら「部屋ごとに1減らす」のような別の時間の物差しが要る
2. **§9 の「予告できないものは入れない」に反する。**
   次の部屋に何がいるか分からないまま毒を抱えて入るのは、読めない不利益
3. **そもそも戦闘外で道具を使う道が無い。** `bagPanel()` を呼んでいるのは
   `drawActs()`（戦闘の行動一覧）の1箇所だけ。持ち越しても対処のしようがない

前は `prepareBattle` の開始時にだけ消していたので、**探索中だけ中途半端に残って**
いた（誰も読まないので害は無かったが、泉の「洗い流された」と
アンチポーションの戦闘外使用という空振りの分岐を生んでいた）。
どちらも下ろし、`finish()` で落とすようにして決まりを本物にした。

**`ITEMS` の `anywhere` は読み手がいない。** 12個の道具に付いているが、
参照している箇所が1つも無い。道具は戦闘中しか使えないので、いまは意味を持たない。
戦闘外で道具を使う道を作るときに初めて効く。消していないのは設計の意図として
残すため。

### 状態異常を解く手段はアンチポーション1つだけ（当面これでよい）

毒・火傷・麻痺・盲目・混乱まで、かかっているものをすべて解く。
料理や他の道具にも解除を持たせるかは、いまは**足さない判断**。
状態異常は戦闘の中だけのもので持続も短いので、1つで足りている。

### 種族・欲望のパッシブで意味が薄いもの

- **一人主義**（エルフ）の条件 `solo` は `PCOND` で `()=>true` と書かれている。
  「同じ列に味方がいなければ」という意図だが、味方が存在しないので常時発動。
  パーティ制を入れたときに初めて意味を持つ。コメントもそう書いてある。
- **エナジードレイン**（強欲）は敵にMPの概念がないため、
  「自分のMP回復＋敵を弱体」という代替効果になっている。

### 〔修正済〕ダイスの演出が盤面からはみ出していた

`.diceOv` は `inset:0` で `.field`（盤面）にぴったり合わせていた。
ところが索敵は「題・2段の d100・結果」で 275px あり、盤面は敵が少ないと 165px しかない。
はみ出した 55px は暗幕の外に出て、上は並び順の帯に、下はコマンドの列に重なっていた。

高さを中身なりに伸ばし（`min-height:100%` ＋ `top:50%` ＋ `translateY(-50%)`）、
暗幕ごと覆うようにした。**新しく演出を足すときは、盤面より高くなっても構わない。**

### 〔修正済〕装備・状態の「閉じる」が「もどる」になっていた

```javascript
$("#charBtn").onclick=charModal;        // ← これ
```

`onclick` に関数をそのまま渡すと、**第1引数にクリックの出来事（Event）が入る**。
`charModal(back)` の `back` は「閉じたあとに戻る先」なので、
出来事が入ると `if(back)` が真になり、閉じるの文言が「もどる」になったうえ、
押すと `back is not a function` で落ちていた（窓は先に閉じるので気づきにくい）。

`()=>charModal()` に直し、受け取る側でも `typeof back!=="function"` を見るようにした。
**`onclick=関数名` は、その関数が第1引数を取るなら必ず包むこと。**

### 〔修正済〕効き目が2つある道具が1つしか効いていなかった

`doItem` が `if(it.heal){…}else if(it.mp){…}else if(it.pow){…}` と
**else でつながっていた**ため、最初に当たった1つで打ち切られていた。

- 石蜜の焼き菓子（`heal:.40, mp:14`）… MP が戻っていなかった
- 沈んだ街の宴（`heal:1, pow:18, def:10`）… 威力も DEF も上がっていなかった

`heal`／`mp`＋`mpr`／`pow`／`def`／`half` をそれぞれ独立した `if` にした。
**道具に効き目を足すときは、必ず独立した `if` にすること。**

`mpr` は「最大MPの割合」で戻す新しい書き方（ハイポーションが使う）。
`mp`（固定値）と両方書けば足される。

### 〔修正済〕記録が残らなかった

`META` がページを閉じると消えていた。図鑑278件・殿堂・弱点の記録・到達階層が
すべて1セッション限りで、「持ち帰るのは知識だけ」という柱が機能していなかった。
`localStorage` に残すようにした（§6「記録の保存」参照）。

---

## 8. 設計上の宿題

- **列の細分化**（後衛1列目・2列目）と**パーティ制**。盤面の作り直しが必要で、
  前衛・後衛条件のパッシブが全部影響を受ける
- **挑戦の解放**（踏破すると新しい区画や高難度版が開く）。殿堂は実装済みだが、そこから先が未着手
- **効果音の外部化**。ファイルの7割を占めている

### 画面の縦を詰めた話（済）

スマホ（390×844）で戦闘画面が **1101px** あり、盤面を見てから
行動を選ぶたびにスクロールが要る状態だった。2箇所を直して **913px** にした。

**大見出しと副題を遊んでいる間は下ろす（−74px）**
`showScreen()` が `body.play` を付け外しし、`#brand` を隠す。
表題を出すのはホームと遊び方だけ。歯車（`position:fixed`）とぶつからないよう、
上の行に `padding-right` を入れて右端を空けている。
これに合わせて `.backbtn` と `.secttl` は `.secbar` の横並びにした
（`.secttl` の `margin:-28px` で重ねる作りは、見出しが長いとぶつかる）。

**盤面は使う段だけ描く（−112px）**
3段 × 5列を常に敷いていたが、**68 の遭遇のうち 34 は1段で足りる**（2段が29、3段は5）。
`draw()` が中身のある段だけを描くようになった。

段数は「倒れた敵も数に入れて」決める。倒しても縮まないので、
戦闘の途中で**行動一覧の位置が動かない**（押し間違いを防ぐ）。

同じ理由で、倒れた前列の敵は盤面に残す（後列は元からそう）。
`unitCell` が `dead` で灰色にし、`canTarget` が的から外す。
予告アイコンの枠も `visibility:hidden` で空けたままにする。

**枠は3つしかないので `boardList()` を通すこと。**
倒れた者を残すのは空きがあるときだけ。生きている者を押し出すと、
その敵は絵が出ず**的として押せなくなる**（仲間を呼ぶ敵がいると起きる）。

結果（実測）：

| | 盤面の段 | 行動一覧の下端 | 画面 |
|---|---|---|---|
| 敵1体 | 1段 | 563px | 収まる |
| 敵2体 | 2段 | 714px | 収まる |
| 敵3体 | 3段 | 741px | 収まる（余裕は少ない）|

390×745（実機のバーを引いた高さ）での値。

### 探索の地図まわり（済）

**地図がいつも入口を映していた。**
`drawFloor()` は**どの呼び出し元でも `showScreen("floor")` より先**に走る。
このとき `#floor` はまだ `hide` なので `#mapbox` の `clientWidth` も
`clientHeight` も 0 になり、`focusY()` が最下部（＝入口）を返していた。
後半になるほど現在地が画面外になり、毎回スクロールで戻る必要があった。

`setFocus()` を挟み、寸法が取れないうちは `requestAnimationFrame` で
描画を待ってから位置を決めるようにした。`introScroll()` も同じ。

**この「先に描いてから見せる」順序はどの画面も同じなので、
寸法を読む処理を足すときは必ず気をつけること。**

**「やめておく」がスクロールしないと押せなかった。**
`#mapinfo`（部屋を選んだときの窓）は地図の下にあり、地図が `68vh` あるぶん
画面からはみ出していた。実測で下端が 896〜925px、画面は 844px。
`position:sticky; bottom:0` で画面の下に貼り付けた。

結果（390×745・実機のバーを引いた高さ）：

| 段 | 地図のスクロール | 現在地 | 進める先 |
|---|---|---|---|
| 1〜4段目 | 最下部（入口が近いので正しい）| 見える | 見える |
| 5段目以降 | 現在地に追従 | 見える | 見える |
| 15段目 | 最上部 | 見える | 見える |

「やめておく」はどの段・どの画面幅でも画面の下端に出る。

### 戦闘画面の情報の出し方（済）

**行動順の帯（`drawOrder`）**
顔アイコン1枚のふだにして、行動アイコンを顔の右上に重ねた。名前の行は下ろし、
`title` と盤面のほうに任せている。帯の高さは **76px → 52px**。

**次のラウンドも並べる。** 並び順は DEX で決まるので前もって出せる
（`orderOf()` を `buildQueue()` と共有している）。各敵の次の技は `f.next` に入っている。
`R2 次` の区切りを挟んで並べ、薄く・枠を破線にして今のラウンドと区別する。
あふれる分は `#ordList` が横に流す。

**盤面をタップすると詳細が開く（`bindCells`）**
敵なら `foeModal`、自分なら `charModal`。
**的を選んでいる最中は何もしない**（`pending` を見て早期に戻る）。
選べるマスの `onclick` は `bindTargets` があとから上書きするので、
`draw()` の中では `bindCells()` → `bindTargets()` の順に呼ぶこと。

**詳細（`drawDetail`）**
弱点・耐性（`knownElem`）、図鑑3段目以降の DEF と速、状態異常の印を足した。
次のターンの行動は元から出ていた。

### 2026-08-31 の調整（後半の手応え）

踏破の報告で「後半の敵の攻撃力やHPが足りない。ボスも物足りない」と言われた。
当時の `depthMul` はこうだった。

```javascript
if(RUN&&RUN.boss)return 1;                        // ボスは 1.0
return r<2?0.9:(r<5?1:(r<10?1.14:1.3));           // 区画をまたぐ伸びが無い
```

**区画をまたぐ重みがまったく無く**、アトランティスの敵も草原の敵と同じ倍率だった。
そのあいだにこちらは15レベル上がって装備も揃う。
**ボスは 1.0** で、同じ区画の奥の通常部屋（1.3）より軽いという逆転もあった。

α0.0015 で階層ごとの重み（1.0 / 1.34 / 1.68 / 2.02）とボス 1.3 を入れ、
α0.0025 で `tools/sim.js` で測ったうえ **敵の素の数値**を直した。
倍率は全区画に一律にかかるが、素の数値なら区画を狙って直せる。

測って見つかった逆転（区画ごとの平均）

| 区画 | 階層 | 直す前 | 直したあと |
|---|---|---|---|
| 原始の草原 | 1 | 78/25/6 | そのまま |
| 苔むした洞窟 | 2 | **74/23/6**（第1階層より弱い） | 90/27/8 |
| 水没した回廊 | 3 | 149/36/15 | 165/39/17 |
| 水没都市アトランティス | 4 | **120/40/9**（第3階層より HP も DEF も低い） | 174/46/14 |

ボスの HP … 260 / 300 / 440 / 540 → 260 / **380** / **510** / **820**

**スライムとゴブリンは原始の草原と苔むした洞窟の両方に出る**ので触っていない。
洞窟のほうは その区画だけに出る4体（ラット・弓ゴブリン・ホブゴブリン・
ゴブリンキャスター）とボスで持ち上げた。

通常戦の余裕（`tools/sim.js`）は 5.4→8.3→3.6→2.1 から
**5.1→5.3→2.8→1.1** になった。第2階層に出ていた山（第1階層より楽）も消えた。

### 新しい区画を作るときの手順（世界樹の森でやったこと）

1. **`PXFOE` に敵のドット絵**（16×20 の `[x,y,w,h,色]`）。まず絵を並べて見る。
   1体ずつ 72px で並べたシートを撮ると、読めない絵がすぐ分かる
   （ケンタウロスとハーピーは一度描き直した）
2. **`MATS` と `PXMAT` に素材**。`--check` が絵の無い素材を弾く
3. **`FOE` に敵**。`d`（設定文）と族の値をそろえる。数値は tier の並びに合わせる
4. **`ENCS` に遭遇**、**`AREAS` の出現表**、**`PXDUN` に情景**
5. **`AREAS` から `wip:true` を外す**
6. `node tools/probe.js --check` → `node tools/sim.js --forest`（森の道）

**数値合わせは「1体ずつの 被/ターン」で見る。** 遭遇まるごとの余裕だけ見ていると、
どこが重いのか分からない。同じ主人公・同じ深さで既存の同 tier と並べると、
外れている個体がすぐ出る（レッドキャップ 79.7 とエルダートレント 131.3 が
回廊の最大 50.6／87 を大きく超えていた）。

**攻撃の重みの割合もそろえる。** 既存の第3階層は攻撃が重みの 67% で、
最初に作ったときは 53% しかなく、HP を合わせても手応えが出なかった。

**行動の値の名前を間違えやすい。** `veil` は `block`（前列全体の DEF）を取るのに
`dur` を渡していて、戦闘のログに「防御 +undefined」と出た。
**必ず一度は実際に戦って、ログを読むこと。**
| 手 | 要る値 | すること |
|---|---|---|
| `atk` | `dice` か `diceRand`（`pow` は増減） | 攻撃 |
| `guard` | `block` | 自分の DEF |
| `veil` | `block` | **前列すべて**の DEF |
| `buff` | `pow` か `dex` | 自分の威力／速さ。`dex` はこの戦闘のあいだ続く |
| `cheer` | `pow` | 前列すべての威力 |
| `grow` | `hp` | **最大HPごと増える。** 深さ倍率が掛かる |
| `regen` | `heal` | HP を戻す |
| `summon` | `who` | 呼ぶ |
| `hex` | curse/weak/slow/blind/dice/**poison** のどれか | 状態異常 |

**`seq:n` は n 手番目に確定で出る。** `w` は要らない（乱数の候補からも外れる）。
スプリガンの「膨れ上がる」がこれ。**索敵に勝てば膨れる前に落とせる**という遊びになる。

〔直した〕`seq` に `!alone` が付いていて、**ひとりでいる敵の `seq` が黙って握り潰されて**
いた。既存の `seq` 持ち（ゴブリンキャスター・シャーマン）は `solo` の手も持つので
上の枝で返ってしまい、この条件が効いたことは一度も無かった。

**`tools/sim.js` は `grow` を数えない。** スプリガンは実際より弱く出る。

### 1周の手応えの測り方（`tools/sim.js`）

`tools/balance.js` は職業×種族の**差**を見る物差しで、式を写して持っている。
写した式は本体から離れていき、**これまで2度まちがえた**（前列の被害減・回避）。

`tools/sim.js` はそれを避けるため、**ゲーム本体の関数をそのまま呼ぶ**。
Playwright で `index.html` を開き、ページの中で
`makeMe` / `genAct` / `expFor` / `makeFoes` / `powOf` / `threshold` /
`perHit` / `hitDmg` / `defOf` を使って測る。

```
node tools/sim.js          いまの数値で、区画ごとの手応えを出す
node tools/sim.js --try     案を振って比べる
node tools/sim.js --json    機械向け
```

先に `python3 -m http.server 8765` を上げておくこと（`SIM_URL` で変えられる）。

**出しているもの**

| | 意味 |
|---|---|
| Lv | その区画のボスに挑むころの値。本物の `genAct` で道を引き、通った部屋の経験を数える |
| 倒すまで | 遭遇まるごとの HP ÷ こちらの1ターンの見込みダメージ |
| 倒れるまで | こちらの最大HP ÷ 敵の集団の1ラウンドの見込みダメージ |
| 余裕 | 倒れるまで ÷ 倒すまで。1 なら相打ち、大きいほど楽 |

**入れていないもの** … スキル・パッシブ・道具・休息・図鑑の命中補正。
Lv24 では技6つとパッシブ10個を選んでいるので、
**実際の手応えはここで出る数字よりかなり楽になる。**
数字そのものではなく、階層どうし・種類どうしの**並び**を見るために使う。

`--try` で振れる栓は3つ。

| 栓 | いま | どこ |
|---|---|---|
| `step` | 0.34 | `depthAt(tier)=1+(tier-1)*step` |
| `boss` | 1.3 | `depthMul()` のボスの枝 |
| `strf` | 0.6 | `makeFoes` の `STR*(1+(dm-1)*strf)`。HP は 1.0 で伸びる |

**敵は毎ターン殴ってこない。** `chooseTele` は防御・溜め・呼び寄せも重みで選ぶので、
被害は**全部の手**の重みで割ること。攻撃の手だけで割ると多く見積もる（一度まちがえた）。

### 数値の測り方（`tools/balance.js`）

職業×種族16通りの強さを、**実際の敵の数値**を使って測る。

```
node tools/balance.js         いまの数値で測る
node tools/balance.js --try   調整案を並べて比べる（案は main の中で編む）
```

見ているもの。

```
火力  1ターンの見込みダメージ。その階層に出る敵の DEF の中央値と、出目の期待値を通す
      索敵で勝ったぶんの初撃ボーナスも、遭遇まるごとを倒すターン数で薄めて足す
耐久  その階層の敵の攻撃を受けて倒れるまでのターン数
総合  火力 × 耐久（1戦でどれだけ働けるかの目安）
```

**入れてある補正。**

- **遠隔武器の職業は後衛に居座れる**ので、前列の敵（全体の75%）からの被害を4割減らす
- **索敵**（`doScout`）。`floor(DEX/13)` が索敵ダイスに乗り、勝ち幅が
  最初の攻撃のダイスになる。上限 +3。二項分布の畳み込みで期待値を出している

**この物差しで見ていないもの。** 使うときは頭に入れておくこと。

- スキルとパッシブ（1周で17回習得する。実際の強さの大きな部分）
- MP の制約（ウィザードはここで縛られる）
- 装備で伸びる分
- 命中閾値の職業差（DEX 差で変わる）

数字はあくまで**職業と種族の素の差**を見るためのもの。

### 2026-08-31 の調整（スカウトと硬さ）

6つまとめて動かした。`node tools/balance.js --before` で調整前の数字が出せる。

| 何を | いくつ → いくつ | なぜ |
|---|---|---|
| `GROW.scout` | `{DEX:5,STR:2,VIT:2}` → `{STR:4,DEX:3,VIT:2}` | 短剣が STR 依存なのに DEX を伸ばしていた |
| `JOB.scout.st.STR` | 26 → 30 | 成長を変えても Lv1 は動かないため |
| 短剣系7本の `mul` | ×1.15 | 1発が小さいほど `perHit` が DEF に削られる |
| `JOB.knight.st.DEF` | 10 → 8 | 敵の攻撃が通らず「攻撃してこない」ように見えていた |
| `RACE.dwarf.st.DEF` | 5 → 3 | 同上。ドワーフのナイトは1発 6 しか受けていなかった |
| `GROW.mage` | `{INT:5,VIT:3}` → `{INT:5,VIT:2}` | 全階層で最上位だった |

**武器の `stat` は STR のまま。**（DEX 依存にするのは却下済み。`IDEAS.md`）

結果。ばらつき（総合の最大÷最小）:

| | 調整前 | 調整後 |
|---|---|---|
| Lv1 | 5.42倍 | **2.58倍** |
| Lv10 | 5.50倍 | **1.93倍** |
| Lv16 | 4.59倍 | **1.56倍** |

Lv10 のエルフスカウトは 27% → **64%**、ドワーフナイトは 139% → 113%。
実機でも、エルフスカウトの1発が 11 → 18、スライムがドワーフのナイトに与える1発が 6 → 11 になった。

**残っている偏りはエルフの物理**（アーチャー 69%・スカウト 64%）。
`RACE.elf.st.STR` が −3 で、弓も短剣も STR 依存のため。
エルフは魔法の種族という建て付けなので、いまは触っていない。

### 2026-08-30 の調整

**`GROW.knight` の VIT を 5 → 4 に下げただけ。** 耐久が突出していた。
成長点は 8 になり、ウィザードと同じ。Lv10 の HP が 387 → 351。

**スカウトの武器を DEX 依存にする案は却下された。武器は STR 依存のまま。**

### いまの数字（Lv10・2階層の敵）

| 職業 | 火力 | 耐久 | 総合 |
|---|---|---|---|
| ウィザード | 118〜162% | 89〜127% | **140〜146%** |
| ナイト | 87〜96% | 105〜148% | 89〜139% |
| アーチャー | 89〜120% | 82〜120% | 71〜137% |
| スカウト | **44〜64%** | 63〜93% | **27〜59%** |

ばらつき（最大÷最小の総合）は Lv1 5.42倍 / Lv10 5.50倍 / Lv16 4.59倍。

### スカウトが弱い理由（2026-08-31 に対処済み）

3つ重なっている。

1. **`GROW.scout` は DEX 中心（DEX5 STR2 VIT2）なのに、短剣系7本の威力は STR 依存。**
   レベルを上げても威力が伸びない
2. **手数の多い武器は敵の DEF に弱い。** `perHit` が `max(威力×25%, 威力 − DEF)` なので、
   1発が小さいほど削られる。DEF12 の敵に対して、スカウトの1ヒットは
   ナイトの半分以下になる
3. **索敵のボーナスは +3 が上限。** DEX が高くても、そこから先は捨てている。
   Lv10 のヒュームスカウトは差 5.5 のうち 3 しか受け取れない（2.5 を捨てる）。
   アーチャーも同様に上限に張り付くので、**DEX の差が索敵では埋まらない**

**「DEX は索敵で報われているから放っておいてよい」というのは誤り。** 上限で頭打ちになる。

直すなら成長側から。測った案（武器は STR のまま）。

| 案 | スカウトの総合(Lv10) | ばらつき |
|---|---|---|
| いまここ | 42% | 5.50倍 |
| `GROW.scout` を STR4 DEX3 VIT2 に | 約68% | 約3.1倍 |
| ＋短剣系の倍率 1.15倍 | 約78% | 約2.6倍 |

**幸運は `GROW` に入れないと決めた**ので（§3「幸運」）、配分の組み直しは要らなくなった。
残るのは技能値のほう。技能値を入れるなら一緒に決めるほうが手戻りが少ない（`IDEAS.md` §1）。

### 残っている偏り

- **ウィザードが 140〜146% で最上位。** 下げるなら `GROW.mage` の VIT 3 → 2。
  ただし MP の制約を物差しが見ていない
- **職業より種族の差が大きい。** ドワーフは DEF 15 と VIT 14、
  エルフは STR −3 なのに弓が STR 依存。DEF はレベルで伸びないので最後まで効く

### 手をつける順の案

1. 苔むした洞窟・水没した回廊・水没都市アトランティスに BGM を入れる（いまは無音）
2. 世界樹の森・魔法の森・夜空の洞窟の中身（いまは開発中の置き石）
3. 後半の敵とボスの手応え（tier で重さを付け直したが未実測）
4. 効果音の外部化（ファイルの67%を占めている）
5. エルフの物理をどうするか（上の「2026-08-31 の調整」の末尾）

まだ作っていないものの構想は `IDEAS.md` に置いてある。

---

## 9. 設計の原則

作業中に何度か立ち返った判断基準。

**強さは持ち帰らせない。持ち帰るのは知識だけ。**
持ち帰り通貨や継承を入れると、難易度が「何周やったか」で決まり、1周の設計が意味を失う。
解放するなら**選択肢の幅**であって、強さではない。

1周で何が残り、何が消えるかの実際（`makeMe()` が毎回まっさらに作り直す）：

| 周をまたいで残る | 1周かぎりで消える |
|---|---|
| 図鑑（`META.codex` `META.seen` `META.mats` `META.rooms`） | 素材・食材（`me.mats`） |
| 弱点の記録（`META.elem`） | 金貨（`me.gold`）・道具（`me.bag`） |
| 殿堂（`META.hall`）・到達階層（`META.best`） | 装備と予備（`me.eq` `me.stash`） |
| 名前を知った区画（`META.areas`） | レベルと経験（`me.lv` `me.exp`）・技・パッシブ |

**素材も経験も残らない。** 残るのは「その素材を見たことがある」という図鑑の印だけ。
遊び方の画面がここを曖昧に書いていて、素材が持ち帰れると読める文言になっていた（α0.0016 で直した）。

**固定値ではなく倍率で書く。**
能力値が1周で2〜3倍に伸びるので、固定値のボーナスは終盤に薄まる。

**同じ効果を複数の軸に置かない。**
職業と種族と欲望で同じ効果があると、選ぶ意味が消える。
6の目はスカウト、ダイス増加もスカウト、DEF強化はナイトとドワーフ、といった住み分けをしている。

**予告できないものは入れない。**
敵の行動は2ターン先まで見える。プレイヤーが読めない不利益は避ける。

---

## 10. 開発時の注意

### 文字列置換で編集するときは範囲に気をつける

このファイルは 5447 行あり、似た記述が多い。`s.index()` で範囲を取るとき、
終端が始端より前にあると空文字列になり、`replace('', x)` が
**全文字の間に挿入される**という事故が起きた。置換前に範囲の妥当性を確認すること。

行番号で切るより、`/* ===== 区切り ===== */` のコメント帯を目印にしたほうが安全。

### 検証は Node でできる

`tools/probe.js` が DOM のダミーを与えて `<script>` を読み込む。

```
node tools/probe.js            データの件数を一覧する
node tools/probe.js --check    参照の整合をチェックする（不整合があれば終了コード 1）
```

`--check` が見ているもの。

- 敵のドット絵・素材・属性・`summon` の呼び先が実在するか
- 遭遇に並んだ敵、階層に並んだ遭遇が実在するか
- どの階層からも出現しない遭遇が残っていないか
- パッシブの発動条件が `PCOND` にあるか
- スキルの属性・状態異常が実在するか
- 図鑑の `id` が重複していないか
- レシピの材料と出来上がりが実在するか

自分で数式を叩きたいときは `load()` を呼ぶ。

```javascript
const api = require("./tools/probe.js").load();
api.perHit(120, 8);            // 1ヒットの威力
api.elemMul("fire", api.FOE.p_sl);   // 属性倍率
```

取り出す名前を増やしたいときは `probe.js` の `EXPORTS` に足す。

**注意**：`load()` はゲームの初期化まで走らせる。副作用のある関数
（`draw` `doAct` など）は DOM のダミー越しに動くので、結果を信用しないこと。
純粋な計算とデータだけを見る道具として使う。
